# Log

## 2026-07-25 — declare the `status:` vocabulary divergence

- **The owner's decision: keep `status:` as ADR workflow state, deliberately.**
  OKF 0.2 §5.4 specifies `status` as a lifecycle field (`draft` / `stable` /
  `deprecated`), which collides with the 27 `accepted` ADRs here and with the
  `proposed`/`accepted` values that `scripts/okf pending` and
  `check-okf-version.sh` read — in this repo and in every repo installed from it.
  Reconciling the two would break both readers everywhere. Declined, and now
  declared in [index.md](index.md) instead of left implicit.
- **Conformance is unaffected**: §11 requires only parseable frontmatter, a
  non-empty `type`, and reserved filenames matching §8/§9, none of which involve
  `status`.
- **Why declaring it matters.** §4.1 requires consumers to tolerate unknown
  *types*, and §11 forbids rejecting a bundle over unknown `type` *values* or
  unknown *keys* — neither covers an out-of-vocabulary value of a *known* key, and
  §5.4 defines its vocabulary without saying how to treat anything outside it. The
  behavior is undefined rather than protected, so a consumer filtering on
  lifecycle could read `accepted` as "not stable". Nothing does today.
- Recorded for a future revisit: if machine-readable lifecycle is wanted, add a
  separate key rather than renaming (§4.1 protects additive keys). Revisit
  triggers are a real OKF consumer filtering on `status`, or OKF 0.3 adding
  conformance requirements around lifecycle.
- **Noticed, not changed:** this bundle root declares `kit_version: "0.3.0"` while
  the kit publishes 0.3.14. It has gone unnoticed because this repo has no
  `.claude/hooks/`, so its own SessionStart drift check never runs. Whether the
  source kit should carry a `kit_version` stamp at all is a real question — ADR
  0010 makes `VERSION` source-only and has installers stamp *targets* — so it is
  left for the owner rather than silently corrected here.

## 2026-07-25 — ADR 0027 accepted

- [ADR 0027](adr/0027-okf-0.2-frontmatter-emission.md) is **accepted** at the
  owner's direction. `status: proposed` → `status: accepted`, the body `# Status`
  section records the date, and the index entry drops its **proposed** marker.
- No implementation change: under the propose-then-implement policy the work
  shipped in 0.3.14 already. Acceptance makes it binding — the emitted provenance
  contract (`generated: { by: process:okf-scaffold, at: <ISO 8601 UTC> }`, the
  `process:<id>` actor choice, and installers stamping `okf_version: "0.2"`) now
  governs future work until a new ADR supersedes it.
- `bash scripts/okf pending` reports no proposed ADRs.

## 2026-07-25

- Kit 0.3.14: the kit stops emitting retired OKF v0.1 frontmatter. OKF 0.2
  supersedes `timestamp` with `generated.at` (SPEC.md §13.1), and the kit was on
  the wrong side of it in three places at once — `scripts/okf` wrote `timestamp:`
  from all three scaffold sites (`draft`, `new-adr`, `new-spec`), the three
  templates carried `timestamp:` in their own frontmatter and documented
  `okf_version: "0.1"` and "OKF v0.1", and **both** installers hardcoded
  `okf_version: "0.1"` when writing a bundle root. Recorded as proposed
  [ADR 0027](adr/0027-okf-0.2-frontmatter-emission.md); the owner accepts,
  amends, or reverts.
- The compounding was the point. A repo installed from 0.3.13 was born stamped
  0.1 and fired an OKF drift note on its first session, and a repo that had
  already migrated to 0.2 was dragged back to v0.1 field names by its next
  `okf new-adr`. The spec-drift dogfood repo migrated its bundle to 0.2 earlier
  the same day and would have regressed on its next scaffolded document — this is
  the half of that migration that no downstream repo can fix for itself.
- The emitted form is
  `generated: { by: process:okf-scaffold, at: <ISO 8601 UTC> }`. The instant
  recorded is unchanged; only the field shape moves. The actor is the §7
  `process:<id>` form because a scaffold skeleton is machine-produced — ADR 0027
  records why `<producer>/<version>` lost (`VERSION` is source-only and never
  installed per ADR 0010, so the helper would have to parse a `kit_version` stamp
  that ADR 0018 lets brownfield repos omit) and why `human:` lost (§7 reserves
  the prefix for hand-authored content because §5.3 derives trust tiers from it,
  so claiming it would inflate every generated draft to human-reviewed).
- Verified by scaffolding, not by inspection: `okf new-adr` and `okf new-spec`
  were run against this repo and the emitted frontmatter was parsed with PyYAML,
  which returned a real two-key mapping (`by` a string, `at` a datetime) — the
  unquoted `process:okf-scaffold` was the risk, since a colon inside a flow
  mapping can parse as a nested key. The probe documents were then removed. That
  same parse caught broken YAML in ADR 0027's own `description`, where an
  unquoted `generated: { by, at }` in prose was being read as a nested mapping;
  the description is now quoted.
- The template frontmatter uses `by: "human:[owner name]"` rather than the
  scaffolder's process actor: those files are hand-maintained playbooks, not
  generated skeletons. The quoting is load-bearing — an unquoted `[owner name]`
  placeholder parses as a YAML list.
- Templates also had two stale instructions the field rename exposed: the header
  comment told the owner to "update the timestamp", and the version-policy
  section used "0.1 → 0.2" as its worked example of a minor bump, which now reads
  as a pending migration rather than an illustration. Both corrected.
- `docs/specs/okf-helper-command.md` gains an **Emitted provenance** section
  stating the contract for all three scaffolders in one place, including that
  nothing in the helper rewrites existing documents — a repo carrying legacy
  `timestamp` keys is left alone and migrates on its own schedule.
- `docs/okf-map.yml` now maps ADR 0027 onto the six sources whose emitted
  frontmatter it governs (`scripts/okf`, both installers, all three templates).
  This clears `check-stale` for those files by recording real governance rather
  than leaning on the blanket `docs/log.md` escape hatch.
- **`VERSION` rationale (no doc change warranted).** `check-stale` also flags the
  0.3.13 → 0.3.14 bump against `docs/specs/repo-kit-packaging.md` and
  [ADR 0010](adr/0010-kit-version-stamp.md). Neither needs editing: both describe
  the stamping *mechanism*, which is unchanged, and neither names a version. The
  bump itself is ADR 0010 doing its job — installed behavior changed, so the
  release version moves.
- **ADR 0016 site review (required by the `VERSION` bump): no change needed.**
  `site/` refers to the Open Knowledge Format by name with a link to the spec and
  never states a version, so nothing on the site became inaccurate. The one
  version string in `site/dogfood/index.html` narrates the 0.3.5 → 0.3.12 history
  in past tense and stays correct.
- Not in scope, deliberately: the `# Citations` → `sources` half of §13.1 (the
  scaffolders never emitted a `# Citations` body list), the additive 0.2 families,
  and **this repo's own `docs/` bundle** — 34 files still carry `timestamp:` and
  `docs/index.md` still declares `okf_version: "0.1"`. That is source-only, ships
  in no install, and bumps no version, so it is sequenced after the installed
  fix. Follow-up work.
- Verification: `make test` (14 smoke targets, parity, links, shellcheck, json),
  `bash scripts/okf check-stale`, `bash scripts/okf adr-suggest`.

## 2026-07-24

- Kit 0.3.13: the playbook now says what to do when two knowledge files disagree,
  at the owner's direction. The grounding rules already carried "when code and
  docs disagree, flag the mismatch, don't silently pick a side" — the code-vs-doc
  case — but said nothing about an accepted ADR and a spec giving different
  requirements for the same thing. The gap surfaced in the spec-drift dogfood
  repo, where a tool built on the kit had to decide what to report for exactly
  that arrangement, and the owner's answer settled it: a contradiction is a defect
  in the documentation, not a precedence question, and the repair is to reconcile
  it in the change that introduces it — update the spec to match the accepted
  decision, or supersede the ADR — never to rank one kind of file above the other.
  Ranking would have been the tempting design, and it contradicts the kit's own
  grounding rule: a standing precedence between file kinds is silently picking a
  side, applied automatically and invisibly, and it would let a stale ADR quietly
  overrule a legitimately updated spec.
- The rule is added to all three playbooks so they cannot drift: `templates/CLAUDE.md`
  and `templates/AGENTS.md` (owner-voiced, "which way is my call"), and this repo's
  own `CLAUDE.md`, since the kit is governed by the same discipline it ships.
  `make parity` confirms the Codex template stays structurally paired.
- The tool half of this decision lives in spec-drift (its ADR 0007: contradictory
  governing documents are reported as `insufficient-evidence` naming the conflict,
  never resolved), verified there against a live provider. This entry is the
  repository-governance half — what a person does once the tool has flagged it.
- `VERSION` 0.3.13 per ADR 0010: `templates/CLAUDE.md` is an installed artifact.
  Site reviewed per ADR 0016 — the only version strings on the site are the
  historical narrative in dogfood story 06, which describes the 0.3.10 → 0.3.12
  upgrade accurately and is not a current-version claim; no page states the
  grounding rules; no copy change. Publish per ADR 0016: project-base Pages
  redeploys on push, then the owner bumps the apex submodule. Verified: `make test`.

- ADR 0018 amended at the owner's direction, correcting the reader list flagged in
  the mapping change below. The correction needed no new judgment, because **the
  ADR already contained the right list**: its Consequences section names the
  duplication sites as "helper, SessionStart hook, `verify-install`, updater,
  harvest" — exactly the five scripts measured in the code, with the hook singular
  — while decision point 2 named a different set that omitted `update-existing-repo`
  and claimed both hooks. The ADR contradicted itself, and point 2 was the outlier.
  Recorded as an amendment at the end of Consequences per the ADR 0013 and 0021
  house style, leaving the original decision text as the historical record and
  naming the duplication line as authoritative. The amendment also states why the
  omission was not harmless — the sentence a reviewer consults to ask whether the
  installer must follow the declared layout answered no, and the installer stamped
  a hardcoded `docs/index.md` for eight days before the 0.3.9 audit caught it, a
  drift `check-stale` could not see either because the ADR was mapped to nothing —
  and confirms the decision itself is untouched: three keys, declared not detected,
  absent block meaning today's paths, with point 3 always binding the updater for
  brownfield detection. No `VERSION` bump: an ADR is source-only and no installed
  artifact changed (ADR 0010). Verified: `make check-docs`.

- ADR 0018 mapped to the scripts it binds, at the owner's direction, closing the
  gap the spec-drift dogfood story left open. The finding was not "the installer
  is unmapped" but something wider: **an accepted ADR governing five scripts was
  mapped to nothing at all**, so `check-stale` never required it to move with any
  of them and no doc-aware reader ever received it. Measured rather than assumed —
  layout-block readers by inspection: `scripts/okf` (2 parser hits),
  `check-okf-version.sh` (2), `verify-install` (2), `harvest-dogfood` (2), and
  `update-existing-repo` (2 parser hits plus 11 stamp-file references, the
  heaviest reader of all). All five now carry the ADR. `check-docs-sync.sh` is
  deliberately **not** mapped: it reads no layout at all, and mapping it would
  manufacture the noise the map header warns against. Verified with both parsers —
  the kit's own `check-stale` reports current, and spec-drift's stricter reader
  parses all 26 mappings and resolves ADR 0018 for the updater but not for the
  docs-sync hook.
- **Flagged, not fixed** (an accepted ADR is the owner's): ADR 0018's point 2
  carries a reader list — "Read by `scripts/okf`, both hooks …, `verify-install`,
  and `harvest-dogfood`" — that is inaccurate in both directions. It omits
  `update-existing-repo`, which reads the block and has stamped through it since
  0.3.9, and it claims "both hooks" when `check-docs-sync.sh` reads no layout.
  The mapping stands on its own regardless, because point 3 of the same decision
  is explicitly "Brownfield adoption in `update-existing-repo`" — the ADR does
  bind the installer, only that one sentence under-describes its own surface. The
  omission is a plausible contributor to the eight-day stamp drift: the sentence a
  reviewer would consult to ask "must the installer follow layout?" said no. An
  amendment correcting the list is recommended and left for the owner.
- No `VERSION` bump: `docs/okf-map.yml` at the kit root is this repo's own map, is
  source-only, and no installed artifact changed (ADR 0010). Verified: `make test`.

- Dogfood story 06 added for spec-drift (site-only), at the owner's direction. The
  repository is the first dogfood target that checks the kit *back*: a CLI built
  inside the kit's loop that reads a diff against the specs and ADRs governing it.
  The story follows the spec's existing shape — what it tested, what it found,
  what changed in the kit — and takes the second row the spec already allows for a
  story whose outcome arrives later. Row one is the 0.3.10 → 0.3.12 upgrade: the
  mirror fix held (no candidate staged), and the filled-playbook suppression did
  not fire on the very repository whose friction motivated it, which became the
  empty-vs-uncomputable delta fix logged below. Row two is the tool turned on the
  kit: the `layout: stamp_file` drift from the kit's own history, reintroduced as
  an ordinary-looking change, was caught in one model call with the same
  consequence the audit had recorded — reported with both of its limits (it judges
  only the change in front of it, and only against records the map points at) and
  with the finding it surfaced for the kit left **open and stated**: ADR 0018 binds
  the installer but is not mapped to it, so nothing mechanical carries it into
  review. Recorded rather than quietly fixed, because the gap is the map discipline
  the kit asks of every adopter, applied to the kit itself. **Site review per ADR
  0016 found one real staleness**, which is what the review exists for: the dogfood
  hero read "four repositories, five stories" and is now "five repositories, six
  stories"; the home page's two dogfood-record links are generic and stay accurate,
  the "evidence is still early" note is untouched, and no page claims the changed
  mechanics. `dateModified` and `article:modified_time` bumped to 2026-07-24 per the
  site-content spec's change rule. **No spec change warranted** — the page follows
  the existing one-section-per-story rule and its second-row allowance verbatim, so
  this entry is the `check-stale` rationale. No `VERSION` bump: `site/` is
  source-only, never installed (ADR 0010). Publish per ADR 0016: project-base Pages
  redeploys on push, then the owner bumps the apex submodule. Verified:
  `make check-docs`.

- Empty-delta and uncomputable-delta separated in the filled-playbook suppression, found live on the spec-drift 0.3.10 → 0.3.12 upgrade — the same repo whose `AGENTS.2.md` friction motivated the 0.3.11 suppression, where the suppression then did not fire. Diagnosis on the live repo: the playbook passes both judgment preconditions (7 landmark headings, zero placeholders from the shared list), so the failure was the third — `write_playbook_template_delta` ends in `if diff -u ... > "$delta"; then rm -f "$delta"; return 1; fi`, and `diff` exits **0 when the two files are identical**. `templates/CLAUDE.md` happens to be byte-identical between 0.3.10 and 0.3.12 (0.3.11 changed the updater and a skill, 0.3.12 added `templates/AGENTS.md`; neither touched the playbook template), so "nothing changed" took the same branch as "an ingredient is missing" and the fallback staged all 192 bracketed lines — the exact outcome 0.3.11 set out to remove, at the exact repo it was harvested from. The two cases want opposite handling and are now distinguished: an *uncomputable* delta leaves the reviewer nothing, so the whole template is still worth staging; an *empty* one is positive proof there is nothing to review. `write_playbook_template_delta` returns a named `PLAYBOOK_DELTA_UNCHANGED` (2) for the identical case, `suppress_playbook_candidate` accepts 0 or 2 (guarded call — a bare invocation would abort under `set -e`), and the two call sites collapse into `note_playbook_suppressed`, which names the delta when one exists and otherwise states the template is unchanged since the stamped release, emitting no advisory that would point at a file that was never written. Smoke: `smoke-candidates` gains the version-crossing, template-unchanged case (no candidate, no delta advisory, the unchanged `Skipped:` line); the uncomputable path keeps its existing coverage in the same simulation's pre-git-history runs. One test-authoring note worth keeping: an initial assertion counted delta files before and after and failed, because two updater runs a second apart share a timestamped backup directory — the assertion, not the fix, was wrong, and the output assertions prove the behavior without it. Installer and validation specs updated; `adr-suggest` quiet (this corrects an implementation defect inside ADR 0018's already-accepted filled-playbook reasoning, not a new boundary). **No `VERSION` bump per ADR 0010**: `update-existing-repo`, the `Makefile`, and the specs are all source-only, and no installed artifact changed — so no ADR 0016 site review is triggered either. One adjacent case examined and deliberately left alone: a *same-version* rerun (stamp equal to `VERSION`) still returns early and stages the whole template for a filled playbook. Suppressing it would need the same-version case to mean "same template", and it does not — a kit clone carrying unreleased template edits at an unchanged `VERSION` is exactly the shape the smoke suite builds, and delta advisories are deliberately release-scoped ("same-version rerun stays delta-silent"). Making that sound means comparing templates rather than trusting the version, which reverses an existing design choice and is the owner's call, not a defect fix. Deliberately left for the owner: the installed `okf-kit-upgrade` skill still tells the reviewer "the delta is then the whole playbook review", which now has a case with no delta to read; correcting that one sentence changes an installed artifact and would require a `VERSION` bump and site review, so it is flagged rather than folded in. Verified: `make test`, plus a live rerun against the real spec-drift playbook stamped one release back (suppression fires, `Skipped:` names the unchanged template, no `AGENTS.2.md` written).

- Kit 0.3.12: the second-agent port renders a curated template instead of re-deriving prose, at the owner's direction, after a stray hand-written `AGENTS.md` turned up untracked in a second (stale) kit clone and Codex reported it. Verified every claim in that report: it asserted Codex resolves Claude-style `@` imports, named three kit files that do not exist (`templates/AGENTS.md`, `Codex OKF Kit Guide.md`, `docs/specs/Codex-hooks.md`), wrote `.Codex` for `.codex`, and put skills under `.Codex/skills` — a find-and-replace over the kit's own 103-line `CLAUDE.md`, structurally identical heading for heading. The owner declined dual-agent-first (the kit stays built for Claude Code; `docs/GOAL.md` and the site are unchanged) and took the bounded increment instead: new source-only `templates/AGENTS.md`, generated once from `templates/CLAUDE.md` with eight explicit mechanic corrections — session-start reads replacing the `@` imports Codex does not resolve, the Codex config surfaces (`.codex/hooks.json`, byte-identical `.codex/hooks/*.sh`, `.agents/skills/` with an honest note that Codex skill support varies), personal overrides outside the repo, the self-describing port paragraph, `AGENTS.2.md` in the candidate wording, and the env-file denial restated as policy for Codex rather than the mechanical gate it is only in Claude Code. `okf-second-agent` step 2 now renders it and carries the owner's filled Master objective and Verification commands across from the live `CLAUDE.md` (template supplies mechanics, live playbook supplies the repo), with the hand-written fallback kept for when no kit clone is at hand. The template is **opt-in**: the installers never write it, so the default install, the nine-entry manifest, and `verify-install` are untouched — asserted by a new negative check in both install simulations (no `AGENTS.md`, no `templates/AGENTS.md` in a target). `make parity` gains a template gate gone-wrong-proofing exactly the observed defects: no `@` imports or resolution claim, no `.Codex`, every referenced kit file present, and section headings paired with `templates/CLAUDE.md` (the context section the one deliberate rename). All four negative-tested — each mutation fails the gate with a named finding. ADR 0024 amended (the mechanics of a port are template work; scoping and skill adaptation stay judgment). Packaging and validation specs, the map, and the README updated. Site reviewed per ADR 0016: it describes the second agent only as an optional chore-runner and names no port mechanics, so no copy change. `VERSION` 0.3.12 per ADR 0010 (the installed `okf-second-agent` skill changed). Verified: `make test`.

- CI floor pass now asserts it is actually running at the floor, at the owner's direction, after the Dependabot `actions/setup-python` 5→7 bump prompted a look at ADR 0026's floor mechanism. The bump itself is fine — verified on `main`, the floor step reports `Successfully set up CPython (3.9.25)` and the suite passes under it — and the floor is deliberately *not* being raised: ADR 0026 pins it to stock interpreters on supported machines ("revisited annually or when those reference platforms move, not merely when upstream Python EOLs a version"), and those have not moved (this machine's `/usr/bin/python3` is 3.9.6; RHEL 9 ships 3.9 into 2032). What the review did surface is a real gap: the step printed `python3 --version` for a human to eyeball but never checked it, so a changed input or a future action resolving differently would silently turn the floor pass into a duplicate of the run above and nothing would say so. The floor version is now declared once as a workflow-level `FLOOR_PYTHON` variable feeding both the `setup-python` input and a runtime assert, so the two cannot disagree. The assert guards a different property than the pass itself — newer-than-floor *syntax* still has to die at parse time inside `make test`, which no runtime check can catch (the ADR 0026 amendment's point stands); this only proves the pass ran at the floor. Verified both directions against the literal workflow line through the real env-var path: silent under 3.9.6, and `AssertionError: floor pass ran under Python 3.14, not the 3.9 floor` otherwise. Validation-workflow spec updated. No `VERSION` bump: the workflow is source-only, no installed artifact changed (ADR 0010). Verified: `make test`.

## 2026-07-23

- Kit 0.3.11: two recurring upgrade frictions removed, both observed live rather than theorized, at the owner's direction. (1) Mirror-candidate recurrence: the 0.3.5 and 0.3.10 upgrades each staged `.codex/hooks/check-okf-version.2.sh` for a review whose outcome ADR 0021 already dictates — declared mirrors must be byte-identical to the `.claude` hook, so adopting the candidate is the only answer. Root cause: adopting it by hand (`mv` over the original) never records the new digest, so the manifest — ADR 0013's only accepted proof — stayed empty for that path and every later release re-staged the same file. `install_kit_script` now takes an optional second proof, and the declared-mirror sync supplies one: content byte-identical to the *pre-refresh* `.claude/hooks/` original is the mirror invariant holding, not an owner edit, so it refreshes in place and enters the manifest for future runs. Guarded on both sides — the proof applies only when the `.claude` original itself carries the incoming kit content, so an owner-edited original keeps its mirror preserved alongside it, and an owner-edited *mirror* still gets its candidate. A/B against the pre-fix updater on a hand-made mirror: before, a candidate at every release and the mirror left drifted; after, refreshed in place from the first release with none staged. ADR 0013 amended (the manifest is no longer the sole proof). (2) Filled-playbook candidate: today's `AGENTS.2.md` was the kit's 192-line bracketed template staged against a finished 316-line playbook, which reviews to nothing — the actual review cost of the run. A kit-derived playbook with no blanks left now gets the kit-only template delta *instead of* the whole template, with a `Skipped:` line and an advisory naming the delta. Three preconditions, each earning its place: kit-derived (at least four of the playbook's landmark headings, so the delta describes a document the repo actually has — measured 7-8 on every kit-derived fleet playbook and 0 on the hand-written aws `AGENTS.md`, which keeps the whole template), filled (the placeholder list, now an `OKF-SHARED` block byte-identical with `check-placeholders` and enforced by `make parity`, since a bare `\[.*\]` scan over-matches the legitimate `tags: [...]` and `[paths...]` spellings), and a delta actually available. The template's "delete this comment" reminder is deliberately not a disqualifier: repo-pulse and skywatch carry finished playbooks with that line still present, and staging 190 bracketed lines at them helps nobody; `check-placeholders` keeps reporting it. Smoke coverage for both: the hand-made mirror across two consecutive releases plus the preserved-original case in `smoke-mirrors`, and the filled/unfilled/never-kit-derived trio in `smoke-candidates`. Installer and validation specs, the `okf-kit-upgrade` skill, and the README updated; `adr-suggest` quiet (both refine accepted decisions — ADR 0013's proof rule by amendment, ADR 0018's filled-goal reasoning extended to the playbook as 0.3.8 did for the map). Site reviewed per ADR 0016: its candidate copy is generic ("writes review candidates when names or locally edited kit files conflict") and stays accurate; no copy change. `VERSION` 0.3.11 per ADR 0010. Verified: `make test`, plus the A/B repro and a filled-detection sweep over all six live fleet playbooks.

## 2026-07-21

- Site: added an explicit reservation-of-rights note to the content-page footers, at the owner's direction — the one site-review gap the MIT→proprietary relicense left open. That relicense updated `LICENSE` and the packaging spec but recorded no ADR 0016 site review; the review comes up almost clean (the site asserts no license anywhere, so nothing was false), with the single discretionary gap being that the footer said only `© 2026` while the repo is now "All Rights Reserved." The footers of `index.html`, `how-it-works/`, `dogfood/`, and `404.html` now read `... · © 2026 · All rights reserved.` plus the third-party carve-out for the vendored assets (Compact Theme BSD 2-Clause, IBM Plex SIL Open Font License), matching `LICENSE` and not implying any reuse grant; the `now/`/`notes/`/`projects/**` redirect stubs carry no footer and were left alone. The site-content spec gains a footer-rights rule so the note can't silently drift, and the dogfood page's `dateModified`/`article:modified_time` bumped to 2026-07-21 per the spec's change rule. Verified against the current kit state at the same time (skill count still six, enforcement-boundary claim still accurate — the 0.3.10 parity gates and ADR 0026 Python floor are source-kit CI, not installed-target gates — no other stale copy). No `VERSION` bump: `site/` is source-only, never installed (ADR 0010). Publish per ADR 0016: project-base Pages redeploys on push, then the owner bumps the apex submodule. Verified: `make check-docs`.

- ADR 0026 amendment landed at the owner's direction: the Python floor and the bash boundary pinned as gates before any rewrite code exists, so the first rewritten file is born under the constraints rather than retrofitted. Floor: Python 3.9, tracking the oldest interpreter stock supported machines ship (macOS CLT 3.9.x, Debian 11, RHEL 9), revisited annually or when those platforms move; style rules bound with it — `from __future__ import annotations` in every kit Python file, no 3.10+ runtime syntax, stdlib only. Enforcement is mechanical on both fronts: the CI workflow now reruns the full suite under a provisioned Python 3.9 (a runtime version assert cannot catch newer-than-floor syntax — it dies at parse time before any guard runs — so the floor pass is the only mechanism that works, and it already covers today's checkers and installer heredocs), and `make parity` gains two checks — the installed surface (`check-docs-sync.sh`, `check-okf-version.sh`, `scripts/okf`) keeps bash shebangs with zero python3 mentions (the mirror contract's standalone requirement, now unable to erode silently), and every `scripts/*.py` carries the future import. The new-repo smoke additionally asserts a fresh target holds no `.py` file. Both new parity checks negative-tested (a python3 mention in `scripts/okf` and a stripped future import each fail with a named finding). ADR 0026 amended, validation-workflow spec updated. No `VERSION` bump: workflow, Makefile, parity script, and docs are all source-only — no installed artifact changed (ADR 0010). Verified: `make test` locally plus the CI floor pass on the PR.

- Accepted ADR 0026 (Python-stdlib rewrite of the kit-side installer tools) at the owner's direction. The boundary now binds: `update-existing-repo`, `verify-install`, and `create-new-repo` move to Python stdlib in that order when a trigger fires — the next bash-hazard-class bug in a kit-side tool, or a decision to promote the kit beyond dogfood — while the target-installed surface (`check-docs-sync.sh`, `check-okf-version.sh`, `scripts/okf`) stays standalone bash under the mirror contract. No implementation with this acceptance; the ADR itself defers work to its triggers, so this is a docs-only status flip (frontmatter, body Status, index entry, GOAL milestone wording). No `VERSION` bump: no installed artifact changed (ADR 0010). Verified: `make check-docs`.

- Kit 0.3.10: structural-risk gates from the audit's remaining findings, at the owner's direction. (1) Parser consolidation: the layout-block and mirrors-list awk parsers — five and four inline copies whose divergence produced two of the audit's three confirmed bugs — are now shell string constants wrapped in `OKF-SHARED-BEGIN`/`END` markers, byte-identical everywhere (a runtime shared library was rejected: mirrored hooks must stand alone, so the sharing is enforced rather than sourced). The canonical `clean()` now also strips trailing slashes in layout values, harmonizing the layout and mirrors parsers into one text. (2) `make parity` (`scripts/check-parity.py`, stdlib-only, in `make test` and `make check-docs`) converts the specs' "must not drift" prose into build failures: shared blocks byte-identical and present exactly where listed, the Stop hook's exclusion pattern set-equal to `scripts/okf`'s workflow+meta union (the 0.3.9 `.agents/` bug lived precisely in that prose-only rule), the six-skill roster named in the README install-artifacts table row (reworded to literal directory names so the gate bites), both installers (create-new-repo twice: copy plus manifest seed), `verify-install`, and the installer spec, and the ADR 0023 labels present in pinned order in the scripts, the spec, and the ADR. Negative-tested: a mutated shared block, a deleted block, and a dropped exclusion each fail the gate with a named finding (one negative test's `git checkout` cleanup restored the pre-refactor `verify-install`, silently reverting uncommitted work — caught by the gate itself flagging the missing blocks, which is the gate doing its job on its own development). (3) ADR 0026 proposed: Python-stdlib rewrite of the never-installed kit-side tools (`update-existing-repo` first), behind the unmodified smoke suite plus an A/B tree-diff harness, with the target-installed surface (hooks, `scripts/okf`) explicitly staying standalone bash; implementation trigger-gated on the next bash-hazard-class bug or a promote-beyond-dogfood decision, so the ADR binds the boundary now without churning the freshly hardened updater. Specs updated (validation-workflow, repo-kit-packaging, claude-hooks, okf-helper-command), map gains `scripts/check-parity.py`, Makefile help/test/check-docs wired. Site reviewed per ADR 0016: no page claims validation internals; no copy change. `VERSION` 0.3.10 per ADR 0010. Verified: `make test`. Post-push CI finding folded in (the 0.3.7 SC2143 version-skew pattern again): the runner's older ShellCheck raises info-level SC2016 on the shared awk constants' assignments — `$0` inside the single-quoted awk source — while local ShellCheck 0.11.0 stays quiet; each shared block now carries a justified `disable=SC2016` directive naming the skew, inside the marked region so byte-parity holds across all nine copies.

- Kit 0.3.9: fixes from a full-kit audit (every script line-read, specs cross-checked against implementation, suspected bugs confirmed or retracted by scratch-repo repro), at the owner's direction with dual-agent (Claude + Codex) smoothness as the stated priority. Three confirmed bugs fixed: (1) `.agents/` and `Codex.local.md` were missing from the agent-config exclusions in `check-docs-sync.sh` and `scripts/okf` `is_workflow_file` while `.codex/` and `AGENTS.md` were present — a `.agents/skills`-only change hard-blocked the Stop hook in pre-mapping repos, contradicting the `okf-second-agent` skill's explicit "the shipped hooks already treat those paths as agent config" claim; both lists now carry the full second-agent set, keeping the union rule (claude-hooks and okf-helper specs) intact. (2) The updater restamped `kit_version` into a hardcoded `docs/index.md`, ignoring `layout: stamp_file` — a relocated-stamp repo kept its old stamp, gained a spurious second-stamp `docs/index.md`, and drew a SessionStart drift note that never cleared, contradicting the map header's "every kit tool follows these paths"; stamping is now extracted into `stamp_kit_version_in` and lands only in the layout-resolved stamp file (in place when it opens with frontmatter; reported, never candidated, otherwise; no bundle root created beside a relocated stamp). (3) The 0.3.8 undeclared-mirror advisory compared against unnormalized `mirrors:` entries, so a `- .codex/hooks/` declaration (trailing slash) drew a false "was not synced" advisory while the sync itself worked; `map_mirror_dirs` now strips trailing slashes exactly like the SessionStart hook and `verify-install` parsers. Hardening and ambiguity fixes: the SessionStart hook escapes backslashes and double quotes at its single JSON interpolation boundary (an odd candidate filename could previously break the whole payload); `okf draft`'s public-surface scan falls back to plain recursive grep when ripgrep is absent instead of silently reporting nothing; body-status detection (`pending`, hook inbox) matches `- Status:` bullets and `# Status` headings case-insensitively; ADR 0023 amended to state the new-label allowance precisely (labels match by name, not position — the 0.3.8 `Advisories:` insertion made the old "after the existing ones" wording ambiguous); map template header documents that `*` crosses `/` in pattern matching (write `**` for recursive intent); README install-artifacts table now lists all six skills and the updater section describes the `Advisories:` output; specs (claude-hooks, okf-helper-command, installer-scripts, validation-workflow) updated in the same change per the no-drift rules. Smoke coverage added for every fix: `.agents`/`Codex.local.md` no-block and check-stale skip, relocated-stamp restamp (idempotent, no `docs/index.md`, hook stops citing the old version), trailing-slash mirror declaration through sync and advisory, and quoted-filename JSON validity. Audit retraction recorded for honesty: the suspected `new-adr` heredoc title injection was disproven by test (heredoc variable expansion is not re-evaluated) — no change made. Deliberately deferred as known-minor: the non-contiguous candidate-numbering duplicate-content edge and the coarse any-log-change stale escape hatch (documented behavior). Site reviewed per ADR 0016: no page claims the fixed mechanics; no copy change. `VERSION` 0.3.9 per ADR 0010. Verified: `make test`, plus scratch-repo reruns of each original repro.

- Kit 0.3.8: updater review aids and pairing advisories, harvested from the spec-agent-cli 0.3.0→0.3.7 upgrade at the owner's direction. Four items, none decision-shaped (existing decisions cover them; `adr-suggest` quiet): (1) the updater's completion summary gains an `Advisories:` section, appended under ADR 0023's new-label allowance and asserted in the smoke label contract. (2) Undeclared-mirror advisory in the updater itself (ADRs 0021, 0024): the run that refreshes `.claude/hooks/` is the moment an undeclared byte-identical mirror starts drifting, so the updater now names it with the exact `mirrors:` declaration to add — detected against the *pre-refresh* originals, because post-refresh a previous-release mirror matches nothing on disk (the spec-agent-cli case: `.codex/hooks` sat byte-identical and undeclared through the upgrade and was found only by the repo's own parity test). (3) Second-agent skill-pairing advisory (ADR 0024): a skill home that opted into the `okf-*` set (`.agents/skills`, `.codex/skills`) gets the missing skills listed after a release adds one — spec-agent-cli discovered the new `okf-second-agent` skill had unpaired its Codex set only via a red `make check` — plus a line for `okf-*` skills with no `.claude/skills/` counterpart. (4) Playbook template-delta review aid: when a version-crossing run stages or refreshes a playbook candidate, the updater recovers the stamped release's `templates/CLAUDE.md` from the kit clone's git history and writes the kit-only diff to the run's backup directory with an `Advisories:` pointer — the 0.3.7 skill taught this as a manual `git show` technique; the updater has every ingredient and now does it itself (best-effort: no stamp, same version, no history, or an unknown version skip silently; the manual technique stays the fallback in the skill). Also: populated-map template-candidate suppression — a canonical-layout map with an active `- source:` mapping (or one already carrying its `layout:` block) gets no `okf-map.2.yml`, extending ADR 0018's filled-goal reasoning; spec-agent-cli's populated 27-mapping map drew a pure-noise empty-template candidate. Installer and validation specs updated; `okf-kit-upgrade` and `okf-second-agent` skill templates now point at the updater's aids with the manual paths as fallback; smoke coverage in `smoke-existing` (label, suppression, pairing round-trip), `smoke-mirrors` (updater advisory named and silenced by declaration), `smoke-candidates` (fake kit with git release history: delta written, release-scoped, version-equal reruns delta-silent). Site reviewed per ADR 0016: no page claims updater summary mechanics; no copy change. `VERSION` 0.3.8 per ADR 0010. Verified: `make test`, plus a live rerun against a scratch clone of the spec-agent-cli upgrade shape (all four aids fired; all quiet once resolved).

- Relicensed the source repo from MIT to proprietary "All Rights Reserved" at the owner's direction. `LICENSE` now reserves all rights and grants no use/copy/modify/distribute permission, retaining the warranty disclaimer, and adds a third-party carve-out preserving the vendored `site/` licenses (Compact Theme under BSD 2-Clause, IBM Plex fonts under the SIL Open Font License) — those are not the copyright holder's to relicense. The `repo-kit-packaging` spec's one-line license statement updated to match; the historical `docs/log.md` entries naming the MIT license stay as written since they were accurate when logged. No install artifact touched (`LICENSE` is source-only, never copied to targets), so no `VERSION` bump. `adr-suggest` quiet — a governance/ownership statement, but a license swap with no change to the kit's install shape or verification is an owner call recorded in the log rather than an ADR.

## 2026-07-20

- Kit 0.3.7: harvest batch from the 2026-07-19 fleet runs (four upgrades, one brownfield adoption), at the owner's direction. Five items, none decision-shaped: (1) filled-goal heading tolerance — the aws-public-change-feed adoption's `docs/GOAL.md` is unmistakably filled but its `# Goal: <title>` and `## Implementation milestones` headings defeated the exact `# Goal`/`# Milestones` test and drew a pointless `GOAL.2.md`; `goal_is_filled` now accepts titled goal headings at any level and prefixed milestones headings (ADR 0018 spirit), with the brownfield smoke fixture switched to that shape and live-verified against the aws file. (2) A pipefail hazard-class tripwire in `make scan`: any `scripts/*` file setting pipefail fails the build on a non-comment `| grep -q` pipeline — and writing the tripwire immediately caught two live sites the 0.3.6 sweep missed, `install-kit`'s non-empty-target refusal and `harvest-dogfood`'s duplicate-name check, both now capture-based; the last benign `head -1 | grep -qx` stamp probe rewritten too so the scan holds with zero exceptions. (3) The `okf-kit-upgrade` skill's candidate-review step now carries the template-delta technique used three times in the fleet runs: diff the install-era kit template against the live playbook to isolate owner fills, then rebuild from the new candidate. (4) The `okf-second-agent` skill now carries the canonical AGENTS-side reframing of the second-agent paragraph ("this playbook is itself the port"), re-derived by hand in three repos before this. (5) The `okf-adopt` skill's validation step now says to run diff-scoped repo gates against the actual branch range — the aws adoption passed every clean-tree gate locally and then failed CI's base/head site-sync check. Installer and validation specs updated; `adr-suggest` quiet (tolerance refinements and skill enrichment under existing decisions). Post-push CI finding folded in: the runner's older ShellCheck flags the capture-based duplicate-name check as SC2143 ("use grep -q") — the exact form the race fix must avoid — while local ShellCheck 0.11.0 does not; the site carries a justified `disable=SC2143` directive naming the pipefail race, so both versions pass and the scan tripwire stays the enforcing gate. Site reviewed per ADR 0016: no page claims the changed mechanics, no copy change. `VERSION` 0.3.7 per ADR 0010. Verified: `make test`.

## 2026-07-19

- Fixed a pipefail/SIGPIPE race in the shim classifier and the installers' emptiness probes, folded into the unreleased 0.3.6 at the owner's direction, from the first live repo-pulse upgrade attempt. `is_import_shim`'s pure-shim test (`grep -v | grep -q`) ran under the updater's `set -euo pipefail`: the reader exits on its first match and closes the pipe, the writer takes SIGPIPE (exit 141), and pipefail turns that into a bogus "nothing but imports" result — measured at 237 of 300 runs against repo-pulse's 187-line playbook, so the updater staged `AGENTS.2.md` instead of `CLAUDE.2.md` and skipped the playbook candidate entirely (scheduling-dependent, which is why `bash -x` reruns and every small smoke fixture classified correctly). Same hazard class fixed at its three other sites: the `import_line`/`first_heading` extraction now uses `grep -m 1` instead of a `| head -n 1` consumer, `dir_has_md` and the ADR-numbering layout probe capture `find | head -n 1` output instead of piping to `grep -q .`, and `create-new-repo`'s non-empty-target refusal gets the same capture form (there the race could make a non-empty target look empty and defeat the safety check). The brownfield smoke gains a determinism guard: sixty classifier runs under pipefail against a kit-template-sized playbook must all say playbook, and the updater run on that fixture must stage `CLAUDE.2.md` (validation spec updated). Hooks, `scripts/okf`, and `verify-install` run without pipefail, so their early-exit pipelines are unaffected. The botched repo-pulse working tree was reset; its upgrade reruns on the fixed updater. Verified: `make test`, plus a 200-iteration classifier loop against the live repo-pulse playbook (200/200 playbook).

- ADR 0025 accepted by the owner: flipped `status: proposed` to `accepted` in the frontmatter and body Status section, removed the "(proposed)" marker from its index entry, and updated the ADR 0022 amendment line and the GOAL.md milestone marker. The import-before-heading shim classification now binds future work; no implementation change was needed since the classifier, smoke edge, and spec updates landed with the proposal per the propose-then-implement policy. The 0.3.6 entry below stays as written since it was accurate when logged. `bash scripts/okf pending` reports an empty review inbox.
- Kit 0.3.6: import-before-heading shim classification (proposed ADR 0025), at the owner's direction, from the harvest finding the live spec-drift 0.3.5 upgrade logged the same day. That repo's `CLAUDE.md` is functionally a pure redirect ("Edit AGENTS.md, not this file") but carries the kit's preloaded-context block — a `# Preloaded context` heading, two explanation lines, and the three `@docs` imports, 12 non-blank lines — so ADR 0022's heading-free, 10-line commented-shim test called it a real playbook and the updater staged a competing `CLAUDE.2.md` instead of `AGENTS.2.md`. `is_import_shim` now disqualifies on a heading only when the exact `@AGENTS.md` import sits under it (a real playbook imports beneath its own headings; a shim redirects first), and the non-blank budget rises to 15. ADR 0022's tested real-playbook counter-edge is unchanged, its consequences gain an amendment noting the fired revisit trigger, and the brownfield smoke grows a third edge: a spec-drift-shaped fixture (prose, `@AGENTS.md`, heading, prose, three imports) must stage `AGENTS.2.md` with no `CLAUDE.2.md` — plus a direct check that the new rule accepts the real repo's file. Installer spec's shim definition, validation spec's edge list, and the README brownfield paragraph updated. Site reviewed per ADR 0016: the only shim mention (the AWS pure-shim dogfood story) stays accurate, no copy change. `VERSION` 0.3.6 per ADR 0010. Verified: `make test`.

- ADR 0024 accepted by the owner: flipped `status: proposed` to `accepted` in the frontmatter and body Status section, removed the "(proposed)" marker from its index entry, and updated the spec references and the ADR 0021 amendment line from "proposed ADR 0024" to "ADR 0024". The second-agent port skill and the warn-only undeclared-mirror advisories now bind future work; no implementation change was needed since the skill, wiring, advisories, specs, and smoke coverage landed with the proposal per the propose-then-implement policy. The 0.3.5 entry below stays as written since it was accurate when logged. `bash scripts/okf pending` reports an empty review inbox.
- Kit 0.3.5: second-agent port skill and undeclared-mirror advisories (proposed ADR 0024), at the owner's direction, from a fleet review of the four installed targets. The review found the specdrift composition (python-cli-template seed + kit 0.3.2) validated the layering end to end, but also found the recurring gap: all four targets hand-built the same Codex stack from another repo's history, and specdrift's `.codex/hooks/` mirrors predate ADR 0021 so no `mirrors:` list covers them and nothing mechanical said so. Four changes: (1) a sixth installed skill, `okf-second-agent`, carrying the canonical port — owner scoping, playbook port with explicit session-start reads and honest policy-only guardrail wording, hook mirroring with the `mirrors:` declaration, owner-managed skill adaptation, repo-side parity guard, commit-and-log — wired like the other five (both installers, nine-entry manifest, `verify-install`, harvest classification, resident one-liner in the installed playbook's agent-config section); (2) undeclared-mirror advisories in `verify-install` and the SessionStart hook: a byte-identical copy of a kit hook outside `.claude/hooks/` with no covering `mirrors:` entry draws a warn-only note recommending the declaration — detection never drives a sync, recorded as an amendment to ADR 0021's consequences distinguishing detection-for-advice from the rejected detection-for-sync; (3) a SessionStart map-coverage note when the map carries no active mapping (check-stale inert, coarse docs/ gate active), silent once a mapping exists and when no map file is present; (4) the harvest report's second-agent note is now declaration-aware — naming declared mirror dirs or stating none exist — replacing the stale "(re-sync hook mirrors after kit updates)" advice, a deliberate refinement of the 0.3.4 "presence-based stays accurate" judgment now that declared and undeclared mirrors behave differently. The `okf-kit-upgrade` skill's step 4 points at the advisory and the new skill; hooks, installer, packaging, validation, and dogfood-harvest specs updated; README's second-agent section names the skill and the advisory; guide's episodic-procedures sentence corrected from four to six skills; smoke coverage extended (undeclared-advisory fires then silences on declaration in `smoke-mirrors`, map-coverage note both ways in `smoke-hooks`, nine-entry manifest and sixth-skill assertions in `smoke-install`). Site reviewed per ADR 0016: the workflow-skills card now says six skills including the second-agent port; no other page claims changed mechanics. `VERSION` 0.3.5 per ADR 0010. Registering specdrift and spec-agent-cli in the harvest registry deliberately deferred at the owner's direction. Verified: `make test`.

## 2026-07-18

- Kit 0.3.4: ADR 0021 accepted by the owner and implemented (kit-managed second-agent mirror sync), closing the root cause the 0.3.2 harvest left open. As designed in the ADR: a target that keeps byte-identical hook copies for a second agent declares the directories in a top-level `mirrors:` list in `docs/okf-map.yml`; `update-existing-repo` then syncs each declared mirror through the ADR 0013 provenance path — created when absent, refreshed in place (after backup) when provably unedited kit output, preserved with a numbered candidate when owner-edited — with absolute/`..`/`.claude/hooks` entries ignored with a summary note. Hooks only, declared never detected, skills stay owner-managed. `verify-install` gains mirror parity reporting (pass on byte-identical, warn on missing or drifted). Implementation found one latent parser bug: `mapping_pairs` in `scripts/okf` never reset its docs-list state on a new top-level key, so any list following the mappings (like `mirrors:`) would leak its items into the last mapping's docs — fixed with a top-level-key reset rule. The `okf-kit-upgrade` skill's step 4 reframes from "re-sync by hand" to "check declared vs undeclared", the README's second-agent section and the packaging spec's README contract now describe the declared-mirror sync with the target-side parity check as belt-and-suspenders, the starter map documents the `mirrors:` block, and a new `smoke-mirrors` target covers the no-op, fresh sync, idempotency, kit-release in-place refresh, owner-edit candidate path, verify-install reporting, and check-stale cleanliness across a simulated release (validation spec updated). `harvest-dogfood` left unchanged: its second-agent note is presence-based and stays accurate. Site reviewed per ADR 0016: no page claims the manual-mirror behavior, so no copy change. `VERSION` 0.3.4 per ADR 0010. Verified: `make test`.
- Kit 0.3.3: composition hardening from the python-cli-template stack template (accepted ADRs 0022 and 0023), at the owner's direction. That repo — the first stack template announced in the 0.3.2-era site section, now public at v0.2.0 — composes with the kit for real: its `create-project` auto-detects a sibling kit clone, runs `update-existing-repo` on generated projects, relays the installer summary, and guards that relay with a fake-kit CI test. Five findings carried back: (1) its root `CLAUDE.md` is an `@AGENTS.md` shim with three lines of explanatory prose, which failed the pure-shim test and drew a competing `CLAUDE.2.md` — `is_import_shim` now also accepts a commented shim (exact `@AGENTS.md` import, no headings, at most 10 non-blank lines; ADR 0022), with both classification edges smoke-tested; (2) the completion-summary section labels are now a downstream-tested interface, so ADR 0023 pins them (`Created:`/`Updated:`/`Skipped:` + `Backed up:`/`Needs review:` + `Verification run:`) in the installer spec with smoke assertions on both installers; (3) nothing mechanical flagged an unresolved numbered candidate — the template carried the "don't commit unresolved candidates" discipline in its own README — so the SessionStart hook now names manifest-recorded candidates still on disk (JSON-safe note, silent without a manifest) and `verify-install` warns about the same files, with a resident reminder line added to the installed playbook's kit-version policy; (4) the template shipped a real fix for `[`/`]` glob characters in checkout paths, so a new `smoke-paths` target runs both installers, `verify-install`, the helper, and the hook against bracketed-and-spaced target paths (the kit's quoting held — regression guard); (5) its `.env.example` lesson — "copy to `.env`" fails silently when no loader exists — is now a caveat in the installed template's env guardrail. Installer, hooks, and validation specs updated; README and guide synced; `VERSION` 0.3.3 per ADR 0010. Site reviewed per ADR 0016: the stack-templates section describes the layering accurately and names no changed mechanics, so no copy change. Verified: `make test`.

## 2026-07-14

- Framed the "Stack templates" section's copy as optional-but-helpful, at the owner's direction (site-only): the section head now states the goal loop builds a stack from an empty repository on its own and a template is an optional accelerator (not a requirement) that saves re-deriving a trusted toolchain when you build the same kind of project often; the layering card notes the template entry point is a choice and the same brownfield path an ordinary existing repository takes. Structure line in the site-content spec updated to carry the optionality.
- Added a "Stack templates" section to the home page, at the owner's direction (site-only; no `VERSION` bump). It records the layering decision reached with the owner: the kit stays stack-agnostic and carries only the process contract; repeating toolchain decisions (packaging, quality gates, CI) live in separate stack-template repositories — walking skeletons that pass their own gate in their own CI — created first and then layered under the kit via the existing brownfield installer, which needs no changes to compose this way. The section also announces the first template being built next: a Python CLI template seeded once from spec-agent-cli's proven chassis (zero-runtime-dep packaging with extras, ruff + strict mypy, pytest with a branch-coverage floor, repo-health self-tests, CI matrix), with spec-agent-cli itself remaining the dogfood exhibit — its own GOAL.md now records that sole objective on the target side. Site-content spec's index.html structure line updated with the new section between "adopting the kit" and "where the approach helps". No kit mechanics changed, so `adr-suggest` has nothing to flag; the template repo, when built, carries its own decisions. Verified: `make check-docs` plus the structural page check (balanced markup, Compact Theme classes only).
- Proposed ADR 0021: kit-managed second-agent mirror sync. Records the root-cause decision the 0.3.2 harvest left open — the updater manages only `.claude/hooks/` and never touches second-agent mirrors, so the owner re-syncs by hand every upgrade (a manual mechanic the kit's division of labor says belongs in code). The proposed design: a `mirrors:` declaration in `docs/okf-map.yml` naming mirror hook dirs, and `update-existing-repo` refreshing them in place byte-identical to `.claude/hooks/`, reusing ADR 0013's backup-and-provenance path; hooks only, skills stay owner-managed (per-agent substitutions the kit can't reproduce), declared-not-detected. Alternatives weighed: status quo + the 0.3.2 recommendation, a check-only helper, auto-detection, and syncing skills — all rejected in the ADR. `status: proposed`, implementation deferred until accepted (drafted at the owner's direction, not yet built). Scaffolded via `scripts/okf new-adr` (auto-indexed — kit index is list-style). No code change yet, so `make test` unaffected; `adr-suggest` and `check-stale` clean.
- Kit 0.3.2: harvested the dual-agent-mirror finding from the live spec-agent-cli adoption. That repo grew an optional Codex stack (`AGENTS.md`, `.codex/hooks/`, `.agents/skills/`) beside its Claude stack, and the "keep the hook mirrors byte-identical" rule the `okf-kit-upgrade` skill states in prose had no mechanical backstop — a hook edited on the Claude side leaves the Codex mirror stale with no signal. The repo fixed it on its own side (a repo-health test asserting hook byte-parity and `okf-*` skill-set pairing, skipped when the second stack is absent; its ADR-0012). The kit can't ship that test — target stacks vary and the check belongs in the target's own gate per the division of labor — so the harvest is to *recommend the pattern* where the mirror concept already lives: `okf-kit-upgrade` skill step 4 now tells a mirror-maintaining repo to guard the invariant with a check in its own gate rather than relying on discipline, and the README's second-agent section and the packaging spec's README contract carry the same advisory. No new decision (this enriches the episodic skill under ADR 0015, which already sanctions richer-than-resident skill content, and restates the existing owner-keeps-mirrors-in-sync commitment); `adr-suggest` stayed quiet. `VERSION` 0.3.2 per ADR 0010; `check-stale` clean. Root cause left open as a future kit finding: the updater still does not sync second-agent mirrors — a mechanical sync or a shipped parity helper would remove the manual step entirely. Verified: `make test`.
- Kit 0.3.1: owner-substituted templates and a slimmer goal template, from the first live brownfield adoption (spec-agent-cli, adopted with 0.3.0 earlier today — updater clean, `verify-install` and the target's own CI validator green on the first pass). Three findings carried back: (1) `templates/GOAL.md` shipped `owner: [owner name]` frontmatter whose bracket placeholder parses as a YAML list and fails the target's strict frontmatter validator, while `templates/CLAUDE.md` hardcoded the kit author's name — wrong for every adopter but one. Both installers now render the two owner-carrying templates instead of copying them, substituting `[owner name]` with the target's `git config user.name` everywhere they are written (created files and numbered candidates; `&`-safe awk substitution; placeholder kept when no identity is configured, for the goal interview to fill). The templates carry the placeholder inside explicit YAML shapes so the rendered frontmatter is a quoted string and a one-item list (`owner: "Name"`, `deciders: ["Name"]`) — the first scratch run proved bare substitution collapsed `deciders` to a plain string, which the same strict validators reject. Provenance is unaffected: neither file is in the kit-managed manifest set. (2) The goal template's milestone-mechanics paragraph duplicated the playbook's Goal iteration section, and both are preloaded every session — trimmed to a pointer, keeping only the backlog-local rule (edge-input verification) that lives nowhere else. (3) A repo adopted at its target state has no honest backlog to write: the template and the `okf-adopt` skill now name the empty-backlog idiom (dated success-criteria note, goal-met mechanics take over) instead of leaving that case to be re-derived. Installer spec updated; `adr-suggest` stayed quiet (rendering detail under ADR 0006's existing decision, no new boundary); `VERSION` 0.3.1 per ADR 0010. Site: story 05 on the dogfood page gains the live-adoption outcome and these findings, and the feedback-mechanism section notes the harvest loop is now live against a real adopted repo. Verified: `make test` plus scratch-fixture runs of both installers checking the substituted frontmatter.
- Completed the visual light/dark pass on the three edited site pages (`index.html`, `how-it-works/`, `dogfood/`), closing the caveat the 0.3.0 site entries carried. Installed Playwright with its own Chromium in a scratch dir (outside the repo) and drove a headless capture of each page under `emulateMedia({ colorScheme })` for both schemes — a Bash-launched browser process, which sidesteps the in-app Browser pane's navigation denial that blocked earlier sessions. Result: all six renders returned HTTP 200 with zero console errors, and the body background flipped correctly between light (`rgb(245,242,235)`) and dark (`rgb(17,19,18)`), proving the `prefers-color-scheme` branch paints. Read each screenshot: the two new dogfood sections (AWS Alerting System, Spec Agent CLI round two) and the new how-it-works "Division of labor" section render identically in style to the existing card grids in both modes, inline `<code>` legible, no overflow or contrast issues; the home page's division-of-labor paragraph and evidence copy read correctly. No page change needed — this is verification only.
- ADRs 0019 and 0020 accepted by the owner: flipped `status: proposed` to `accepted` in both frontmatter and body Status sections. The brownfield naming-convention tolerance and the `okf-adopt` adoption-pass skill now bind future work; no implementation change was needed since the helper, installer, skill, wiring, specs, site, and smoke coverage all landed with the proposals per the propose-then-implement policy. Confirmed nothing pending against either decision: every consequence item in both ADRs is implemented (0019: the four detection-side moves, `VERSION` 0.3.0, spec updates, smoke cases; 0020: the fifth skill through both installers, verifier, harvest list, eight-entry manifest, playbook one-liner, README/spec/site mentions, smoke assertions), the deferred items (repo-local scaffold templates, migration tooling) are recorded as revisit triggers rather than open work, and `bash scripts/okf pending` reports an empty review inbox. Verified: `make check-docs` for the status flips and `make test` still green.

## 2026-07-13

- Brought the dogfood record up to date with the site's own premise, at the owner's direction (owner-curated set per the site-content spec; no `VERSION` bump — site is source-only). Two new numbered stories on `dogfood/index.html`: "04 · Hand-ported brownfield" for the AWS Alerting System (github.com/lilabrooks/aws-public-change-feed) — the ADR 0018 story that drove 0.2.0's layout tolerance but had never been added to the page — and "05 · Brownfield adoption, round two" for Spec Agent CLI's return as the inverse brownfield (canonical dirs, its own naming/indexes/validator), carrying the ADR 0019/0020 findings and closing the loop on the repo that seeded the kit's first bundle. Copy syncs: the dogfood hero now reads "four repositories, five stories," and the home page's evidence section names all four repos and the brownfield adoption path. The site-content spec's dogfood structure line now allows a repository to return as its own numbered section in chronological order. Verified: `make check-docs` plus the structural page check (balanced markup, theme classes only).
- Added the guided brownfield adoption pass as a fifth installed skill, `okf-adopt` (proposed ADR 0020), at the owner's direction, folded into the unpublished 0.3.0 (no separate bump). The owner asked whether the kit should scan an existing repo thoroughly and hand back a loop-ready result irrespective of structure; the answer keeps the division of labor: the installer stays a deterministic mechanic, and the thorough pass ships as judgment work sequenced by a skill — inventory the repo's knowledge wherever it lives without moving anything, adapt-in-place by default (migration to canonical only at explicit owner direction, with the skill repairing the cross-links, IDs, and CI rules a rename breaks), populate `docs/okf-map.yml` early so `check-stale` becomes the authority, backfill unmapped modules from `bash scripts/okf draft` output promoted per the target repo's own conventions and validators, validate end to end (`verify-install`, `check-stale`, `pending`, the repo's own gates, the stamp), and log the pass including any helper misfires for harvest. A scripted converter was rejected in the ADR: restructuring and rewriting knowledge is content work a Bash script cannot do, and its output would be an unreviewable whole-corpus diff. Wiring: the skill travels like the other four — both installers, `verify-install`, the harvest list, and the manifest (now eight seeded entries) carry it; the installed playbook gains a binding one-liner in the bootstrap section; README, packaging/installer/validation/dogfood-harvest specs, the map (templates/skills/** now also governed by ADR 0020), and the site's workflow-skills card and existing-repo adoption card updated. Smoke coverage: manifest count, skill loop, and playbook mention assertions extended to the fifth skill. Verified: `make test`.
- Documented the division of labor explicitly across the guide, the installed playbook, and the site, at the owner's direction, folded into the unpublished 0.3.0 (no separate bump). The framing: hooks carry what must never be skipped (fixed code paths at session start and stop, not Claude's choice), the `scripts/okf` helper carries what must never vary (numbering, index entries, staleness — computed in code at zero model tokens, instead of recalculated by judgment each session), the preloaded imports carry what every session must know, and Claude's judgment is reserved for content; when a helper misfires on a repo's conventions it declines visibly, the workaround happens in the open, and the friction is harvested back into the kit (the dogfood loop; ADR 0019 is the worked example). Changes: a "why the mechanics live in scripts and hooks" passage in the guide's evergreen-docs section (and its brownfield note now mentions alpha-prefixed numbering); a deterministic-path paragraph in the installed playbook's helper section instructing Claude to prefer the helpers over re-deriving mechanics and to log any workaround for harvest rather than quietly improvising; the home page's "what is verified" section gains the division-of-labor note linking the dogfood record ahead of the enforcement-boundary paragraph; a new "Division of labor" section on the how-it-works page (hooks never skipped / helper never varies / preloads always known / judgment for content, closed by the misfires-are-harvested note) between the loop and decisions sections. Site-content spec structure lines updated for both pages; site reviewed per ADR 0016 (content-only, Compact Theme primitives, no new CSS). No new decision — this articulates ADRs 0003, 0008, and 0014 as installed — so no ADR and `adr-suggest` stayed quiet. Verified: `make test`, plus a structural pass over both pages (balanced markup, new sections and copy present, only Compact Theme classes used). Note: browser rendering was unavailable this session — as with the 0017 re-skin, a visual pass in light and dark modes is worth doing.
- Kit 0.3.0: brownfield naming-convention tolerance (proposed ADR 0019), at the owner's direction, from the assessment of the fourth dogfood target (the spec-agent-cli repo — canonical `docs/specs/`/`docs/adr/` homes and a stamped bundle root, but `adr-0001-*`/`spec-000-*` file names, table-style indexes, and its own frontmatter validator in CI). Four bounded moves, all detection-side with no new `layout:` keys: `new-adr` continues a sole alpha-prefixed numbering convention (`adr-0009-*` yields `adr-0010-*`) while bare-numeric names keep absolute precedence; `new-spec` continues a sole `spec-NNN-*` ID sequence but deliberately never follows bare-numeric chapter names, whose position is editorial (AWS-shape repos keep plain `<slug>.md`); `index_append` declines a table-style index — appending a bullet would corrupt it and generating a row means guessing columns — and the command prints the entry for a manual add; the updater stamps `kit_version` in place (after a backup) when the existing bundle root opens with frontmatter declaring `okf_version`, instead of staging a whole-file `docs/index.2.md` candidate whose only payload is the stamp (the `append_gitignore_entry` precedent). The shared `scan_numbering` helper excludes `index.md` and `*.N.md` review candidates, matching the pending scan. Deferred below the evidence bar: repo-local scaffold templates for repos whose validators require extra frontmatter keys (noted as ADR 0019's revisit trigger). Helper, installer, and validation specs updated; smoke coverage extended (prefixed continuation, bare-over-prefixed precedence, spec-sequence continuation, table-index decline, in-place stamp idempotency); `VERSION` 0.3.0 per ADR 0010. Verified: `make test` plus scratch-fixture runs against copies of the AWS-shaped and spec-agent-cli-shaped targets.
- Re-unified the non-implementation exclusion lists that had drifted between the Stop hook and the helper, at the owner's direction, resolving the background task spawned during the 0.2.0 work and folding it into that same unpublished release (no separate bump; `VERSION` stays 0.2.0). The comment above `is_meta_file()` in `scripts/okf` claimed sync with the hook while the lists differed both ways: `is_workflow_file()` now also skips `.codex/` and `AGENTS.md` — which the hook has excluded as agent config since 0.1.2 but `check-stale` still listed in its unmapped note, so in a repo whose `AGENTS.md` playbook changes most commits (the AWS Alerting System dogfood target) that note was noise every run — and the hook's exclusion regex gains end-anchored `.env.example`, which the helper already treated as repo meta but whose lone change still tripped the docs-sync block. Both specs now state the invariant from their own side (the hook's non-implementation list equals the union of the helper's workflow/agent-config and repo-meta sets; the two must not drift), and the code comments match. Smoke coverage extended: the hook matrix asserts a `.env.example`-only change does not block, and the helper checks assert the unmapped note lists real implementation files while skipping `.codex/` and `AGENTS.md` (validation spec updated). No new decision — this enforces what the 0.1.2 agent-config exclusion already intended, so no ADR and `adr-suggest` stayed quiet. Verified: `make test` and `bash scripts/okf check-stale`.
- ADR 0018 accepted by the owner: flipped `status: proposed` to `accepted` in the ADR frontmatter and body Status section, and removed the "(proposed)" marker from its `docs/adr/index.md` entry. The brownfield layout tolerance and adoption behavior now binds future work; no implementation change was needed since the helper, hook, installer, verifier, and harvest changes plus specs and smoke coverage landed with the proposal per the propose-then-implement policy. `bash scripts/okf pending` reports an empty review inbox.
- Kit 0.2.0: brownfield layout tolerance and adoption (proposed ADR 0018), at the owner's direction, from the read-only assessment of the third dogfood target (the AWS Alerting System repo — specs as numbered chapters under `docs/architecture/specification/`, three-digit body-status ADRs, JSON Schemas as governing contracts, AGENTS.md-first with a CLAUDE.md import shim, no bundle root). Three bounded moves, canonical defaults unchanged:
  - Convention tolerance, no config: `okf pending`, the SessionStart inbox, and the harvest index read frontmatter `status:`, then `- Status:` bullets, then `# Status` sections (lowercased first word); `new-adr` follows existing numbering width and next number (four digits only in empty dirs); `scripts/okf` and the updater seed a newly created index from the knowledge files beside it (never an empty shell); the version hook is silent about `okf_version` when the stamp file is absent or undeclared and skips the network fetch (mirroring `kit_version`); `adr-suggest` classifies `schema` as a contract change, not persistence.
  - Minimal `layout:` block in `docs/okf-map.yml` — `specs_dir`, `adr_dir`, `stamp_file`; drafts and indexes derived — read by `scripts/okf`, both hooks (inline parser copies so mirrored hooks stand alone), `verify-install`, and `harvest-dogfood`. Stop-hook gate change: the crude docs/-prefix gate applies only while the map has no active mapping; with mappings, `check-stale` is the authority, so mapped governing docs may live outside `docs/` (schemas) — unmapped changes downgrade from hard block to the non-blocking note, recorded as a deliberate loosening in the ADR.
  - Updater brownfield adoption: layout detection (existing map block, then bounded filesystem probes), detected layout recorded in the installed/candidate map, seeded indexes, no heading-only candidates for existing `docs/log.md`/indexes, `docs/GOAL.2.md` suppressed for a filled goal, AGENTS.md-first staging (kit playbook as `AGENTS.2.md` with imports rewritten to the layout; missing `CLAUDE.md` created as an import shim), and starter `docs/index.md` links following the layout plus `runbooks/` and `schemas/` when present. Fixed in passing: candidate machinery invoked through pipes lost its summary-array entries to a subshell ("Needs review: none" while the candidate existed) — call sites now use process substitution.
  - `verify-install` honors the layout and warns when an index lists no entries while knowledge files sit beside it; the template map documents the layout block and machine-readable-contract mappings; template/skill wording points at layout-aware defaults. Helper, hooks, installer, packaging, validation, and dogfood-harvest specs updated; `VERSION` 0.2.0 per ADR 0010 (this repo's own stamp caught up from a stale 0.1.1); site reviewed per ADR 0016 with copy updated where it named fixed paths. Verified: `make test` (including the new brownfield smoke target and the re-shaped hook matrix), plus scratch-fixture sanity runs against an AWS-shaped target.

## 2026-07-12

- Re-skinned the whole site onto the vendored Compact Theme (accepted ADR 0017), at the owner's direction, choosing a full re-skin over a palette-only or tokens-onto-styles.css option. Vendored `compact-theme.css`, `compact-theme.js`, and the BSD-2-Clause `compact-theme-LICENSE.txt`/`compact-theme-COPYRIGHT.txt` into `site/`, reusing the IBM Plex fonts already present, and removed the bespoke `styles.css`. Rebuilt every page — `index.html`, `how-it-works/`, `dogfood/`, `404.html`, and the redirect stubs — on the theme's classes (`compact-shell`, `compact-header`/`compact-brand`/`compact-nav`, `compact-hero`, `compact-section`/`compact-section-head`, `compact-grid`/`compact-card`, `compact-tag`, `compact-button`, `compact-footer`, `compact-theme-toggle`). The site now has a system-preference light/dark default plus a header toggle; honesty notes became cards with tags, adoption steps became plain ordered lists, and the flow-diagram SVG was retokened from the old palette to the theme's `--compact-*` tokens so it adapts to both modes. Relative paths (ADR 0016) preserved — verified that assets resolve at both the apex and a `/site/` project base. Because the light palettes were already near-identical (same author), the main gain is dark mode and a maintained design system; the cost is flattening some bespoke components into the theme's leaner vocabulary. Packaging spec, site-content spec (style rules retargeted to the theme; new light/dark rule), okf-map, and the ADR index updated. No `VERSION` bump: `site/` is source-only. Note: browser screenshots were unavailable this session, so light and dark were verified structurally (computed colours, fonts, layout) — a visual pass in both modes is worth doing.
- Made spec/ADR authorship explicit and added a context-window callout on the home page, at the owner's direction. The copy and the flow diagram were passive about who writes the specs and ADRs; now the "proposed architecture decisions" card says the ADRs are ones "Claude writes itself, marked proposed" (owner accepts/revises/rejects), the "the loop" lead adds "Claude authors the specs and writes the ADRs as proposals; you own the goal, and you accept, revise, or reject each proposed ADR," and the diagram's specs/ADRs node reads "Claude writes them; you review." A new callout under the diagram states where the record is preserved and how the kit helps: it lives in git, not the session's fresh, limited context window; the kit preloads only the goal and the spec and ADR indexes at session start and reads the rest from disk on demand, so the record survives every context reset. (The how-it-works page already carried the context-window story in its session-start section; this brings it to the home page.) Site-content spec structure note updated. No `VERSION` bump.
- Added a "the loop" section to `site/index.html` with an inline SVG flow diagram, at the owner's direction, expanding on the framework: the goal → Claude drafts an ordered milestone backlog for the owner to confirm → the per-milestone agent loop (read the governing specs and ADRs, implement and verify, update the spec and propose an ADR when decision-shaped, log and check off, loop to the next) → acceptance pass → goal met with proposed ADRs listed. The diagram is a self-contained SVG themed with the site's palette variables and IBM Plex Mono (no `styles.css` change), responsive, with `role="img"` and a title/desc; verified structurally (8 nodes, 7 arrows plus the cobalt loop-back, no text overflow, all within the viewBox, palette and fonts resolved). The site-content spec now records this as the sanctioned exception to the no-new-CSS rule and documents the index page's new section order. No `VERSION` bump.
- Sharpened the hero to state the kit's category and value: the intro now frames the kit as packaging the goal-driven-development setup you would otherwise assemble by hand (not a productivity claim — evidence is still early), and the signal names the category outright — Claude Code is the agentic coding tool; the kit isn't another agent or tool, it's the memory, skills, hooks, and knowledge that give that loop a goal, guardrails, and a reviewable record. Codified in the site-content spec terminology section. No `VERSION` bump.
- Aligned the site language to Anthropic's own terminology, at the owner's direction, verified against the docs (building-effective-agents, the Claude Code overview, and the Agent SDK overview). Claude Code is named as Anthropic's "agentic coding tool"; the milestone/iteration loop is reframed as Claude Code's "agent loop" (Claude directing its own tools, using test results as environmental feedback / ground truth); the "what is verified" split is cast in Anthropic's workflow-vs-agent terms (deterministic hooks that run as fixed code paths vs. the agentic work Claude does with judgment); and the kit is framed as configuration on Claude Code's extension points (project memory, skills, hooks), not a separate engine. Deliberately avoided "harness" — it is community shorthand the public docs don't use (they say "agentic coding tool"/"engine"). Edits in `site/index.html` and `site/how-it-works/index.html`; the terminology is codified in the site-content spec's new "Terminology (Claude and Anthropic)" section so future edits stay aligned. No `VERSION` bump: source-only site and its governance.
- Documented the one-time apex Pages setup step in ADR 0016 (and the user-site repo's README): a new `*.github.io` repo auto-enables Pages in `legacy` (deploy-from-branch) mode, which serves the repo root and can't reach the `_kit` submodule, so its source must be switched once to GitHub Actions (`gh api -X PUT repos/OWNER/REPO/pages -f build_type=workflow`). Decided against a CI guard: `build_type` persists, making this a one-time setup condition rather than a recurring risk, and there is no clean in-workflow auto-correct short of granting the workflow `pages: write` — so it is documented, not enforced. No `VERSION` bump.
- Reviewed the site for clarity and correctness and acted on the findings, at the owner's direction. Verified the load-bearing external claims by fetching the referenced docs: `/goal` is a real Claude Code built-in (v2.1.139+) and the memory doc confirms CLAUDE.md + auto memory load every session, so both baseline links are valid; spot-checked dogfood specifics against the repos (repo-pulse has exactly five milestones; skywatch has two upstream HTTP APIs, SQLite, SMTP, retention, and both Claude Code and Codex config) — all accurate. Three copy edits to `site/index.html`: sharpened the baseline "why" to name the real gap (the built-in `/goal` is session-scoped and clears; auto memory is machine-local and unversioned; the kit is the versioned complement), replaced the vague "installed playbook" with "CLAUDE.md and on-demand skills," and surfaced the acceptance pass and interrupted-session resume in the milestone-loop card. Restored the former reference page as a new governed page `site/how-it-works/index.html` (owner asked to keep the current `/claude-okf-repo-kit/` reference content), re-styled from its old inline CSS into the editorial component system (page-hero, section-head, steps-list, principle-grid, the dark working-set for the conversation scenarios, limit-note) with relative paths; it carries session start, the goal interview, the iteration loop, decision policy, guardrails with the enforcement caveat, interrupted-session resume, "anatomy of a conversation," and install. Added "How it works" to the nav on every page and a bridge link from the home page's contract section. ADR 0016, the packaging spec, and the site-content spec updated to describe the page (no longer "reference retired"); verified with a subpath serve (styles, fonts, relative nav, the dark section all resolve). No `VERSION` bump: source-only site and its governance.
- Enforced the site's scope boundaries so kit users are never burdened with it, at the owner's direction. Three boundaries: (1) the site is source-only and never installed into a target — already true by the installer allowlist, now mechanically enforced by negative assertions added to `smoke-install` and `smoke-existing` (no `site/`, `.github/workflows/pages.yml`, `VERSION`, `Makefile`, `docs/specs/site-content.md`, ADR 0009/0016, and no `site/` mappings in the installed `docs/okf-map.yml`/candidate), so an installer regression that leaked site machinery fails `make test`; (2) the dogfood page lists only owner-selected repositories and is hand-authored, never generated from the machine-local harvest registry (ADR 0014) or any automation; (3) no automation updates the site or moves the apex — every edit is a maintainer commit and the apex advances only on a manual submodule bump (ADR 0016), so the owner decides which kit changes warrant a site update. Documented as a "Scope and ownership" section in the site-content spec and reflected in the validation-workflow spec. Verified with `make test` (Makefile changed, so the full suite is the right gate). No `VERSION` bump: source-only site governance and tests, nothing installed changes.
- Added a site-content spec and a site-drift discipline so the site tracks the kit as it changes, at the owner's direction. New `docs/specs/site-content.md` governs `site/` content and voice — page structure, the plain/honest editorial voice, the non-negotiable honesty framing (verified-vs-judgment, the `Yes`/`Qualified`/`Mechanical` labels, the enforcement-boundary caveat, "evidence still early"), the accuracy rule (every claim traceable to an ADR/spec/dated log entry), and the style constraints (reuse existing CSS, relative paths per ADR 0016). `docs/okf-map.yml` now maps `site/**` to it, and — the owner's chosen tripwire — lists `site/index.html` alongside the behavior-defining templates (`templates/CLAUDE.md`, `templates/skills/**`) so `check-stale` surfaces the site when those change. That tripwire is a soft reminder by design: `check-stale` unions a source's mapped docs and is satisfied by any one changing or by a `docs/log.md` note, so the real assurance is the release step now written into the packaging spec and root `CLAUDE.md` — a `VERSION` bump requires re-reading and updating `site/`, then publishing per ADR 0016. Spec index updated. No `VERSION` bump: docs and site governance only, `site/` is source-only.
- ADR 0016 accepted by the owner: flipped `status: proposed` to `accepted`. The apex-mirror-via-submodule shape and the standing relative-path constraint on `site/` now bind future work; no further implementation was needed beyond the consolidation already landed below. `bash scripts/okf pending` reports an empty review inbox.
- Consolidated the two kit sites into one governed source (ADR 0016), at the owner's direction. Background: the kit shipped a plainer reference page at the project Pages base (`/claude-okf-repo-kit/`) while the owner's apex user-site repo (`lilabrooks.github.io`) carried a more polished editorial explainer at the apex — two sites for one kit. Because GitHub serves the apex only from the user-site repo, the fix is one source mirrored to both bases, not one deployment: `site/` now holds the editorial site (moved in from the user-site repo — `index.html` explainer, `dogfood/` findings, legacy redirect stubs, `404.html`, external `styles.css`, self-hosted `fonts/`), and the apex mirrors it through a git submodule whose pointer the owner bumps to publish. The former in-repo reference page is retired to git history; the README stays the exhaustive reference. Every path in `site/` was converted from root-relative to relative (and the CSS `url()` font refs), so the identical HTML renders at both `/` and `/claude-okf-repo-kit/`; absolute canonical/OG/schema URLs point at the apex, marking it primary. Packaging spec's site section rewritten to match; `docs/okf-map.yml` adds ADR 0016 to the `site/**` and `pages.yml` mappings. Verified by serving `site/` under a one-level base path (`/site/`) and confirming the stylesheet, self-hosted fonts, cross-page nav, and redirect targets all resolve within the site. No `VERSION` bump: `site/` is source-only and never installed into targets. Superseded the earlier same-day entry that had cross-linked the two separate sites — they are now one source.
- Added `make check-docs`, a fast validation gate for documentation-only changes, prompted by the owner's question about whether accepting an already-implemented ADR (a `status:` flip plus a log append) needs a full `make test`. It doesn't: the installer, hook, and helper smoke simulations have no causal path to a doc edit, while the two checks that legitimately cover `docs/` prose — the local-path/stale-reference scan and the Markdown link check — plus the OKF helper sanity checks (`check-stale`, `pending`, the latter catching a malformed ADR status field) do. `check-docs` runs exactly that subset. The Makefile comment and the validation spec both state the boundary: it is a convenience subset of `make test`, not a replacement — changes touching scripts, templates, `settings.json`, or `VERSION` still require the full suite. Governed by ADR 0004 (validation Makefile); no new decision, so no ADR. No `VERSION` bump: the Makefile is source-only and never installed into targets, so no stamped repo is affected. Documented in the validation spec, this repo's `CLAUDE.md` verification commands, and the README narrower-targets section. Verified with `make test` (the Makefile changed, so the full suite is the right gate here) and by running `make check-docs` itself.
- ADR 0015 accepted by the owner: flipped `status: proposed` to `accepted`. The installed-skills delivery of the four episodic workflow procedures now binds future work; no implementation change was needed — the skills, template diet, installer/verify/harvest wiring, smoke coverage, and doc sync landed with the proposal per the propose-then-implement policy. `bash scripts/okf pending` reports an empty review inbox.

- Kit 0.1.3: workflow procedures as installed skills (proposed ADR 0015), at the owner's direction, unparking the workflow-skills candidate whose evidence gate skywatch resolved as "context optimization" — the owner asked for exactly that optimization (smaller per-session resident context). Four skills sourced from `templates/skills/` install to `.claude/skills/okf-*/SKILL.md`: goal-interview (full script, worked examples, tool-availability check), acceptance-pass (first-time-user checklist and wrong-input catalog), adr-review (accept/reject/request-changes mechanics distilled from the ADR 0011/0013/0014 acceptance flows), kit-upgrade (drift-note walkthrough with candidate review and mirror re-sync). `templates/CLAUDE.md` dieted per the parked design rule: each section compresses to a binding one-liner naming its skill and standing alone if the skill fails to load; guardrails and loop semantics stayed resident untouched. The skills ride the ADR 0013 provenance machinery as kit-managed files: both installers copy them, `create-new-repo` seeds seven manifest entries, the updater refreshes provably-unedited copies in place and preserves edited ones with candidates (smoke-tested), `verify-install` requires them, and the harvest report classifies their drift with a neutral missing label for pre-0.1.3 installs. `templates/GOAL.md` preamble also gained the missing-repo-hygiene candidate source it should have received in the 0.1.2 sync. Packaging spec carries the artifact list, destinations, and the resident-versus-skill delivery split; installer and validation specs, README (install table, manual copy steps, commit list, upgrading), guide, and site describe the same shape. `VERSION` bumped to 0.1.3 per ADR 0010. Verified: `make test` including the new smoke asserts (skills installed with matching frontmatter names, manifest at seven, template pointers present, skill preserve-versus-refresh across a simulated release).
- Cross-repo knowledge query in the harvest helper, at the owner's direction (extends ADR 0014 via the dogfood-harvest spec, per the spec-extension precedent — the registry/read-only/machine-local decision is unchanged): `harvest-dogfood index` rebuilds a derived index beside the registry — one tab-separated line per fact from every registered repo: goal Kind/Problem/Solution lines, milestone checkboxes, spec index entries, per-ADR status/title/description, and truncated dated log bullets — and `query <pattern>` rebuilds then greps it case-insensitively, exiting 1 on no match. Rebuilt on every query, the index can never go stale; like the registry it holds machine-local content and never commits. Motivation: answering a cross-repo question by exploration cost this week's first analysis a session of tool calls; a query now costs one command and tens of tokens per hit. Verified live: 72 entries from the two registered repos; "smtp" returns skywatch's ADR 0004 with status plus the relevant milestone and log history, "homepage" returns repo-pulse's pending ADR 0003. The harvest kit-managed-file report also covers the four new skills.

## 2026-07-11

- Smoke-test hardening, no contract change: every `check-docs-sync.sh` invocation in `make smoke-hooks` now pins stdin to `/dev/null`. The hook legitimately reads stdin for the `stop_hook_active` payload, so an inherited stdin that is a never-closing pipe could stall the suite — one full `make test` run hung that way in a harness shell and was killed at a timeout. The suite is now hermetic regardless of how make is invoked; behavior asserted by the tests is unchanged, so the validation spec needed no edit.
- ADR 0014 accepted by the owner: flipped `status: proposed` to `accepted`. The machine-local harvest registry and read-only delta-report contract now bind future maintainer tooling; no implementation change was needed — the helper, spec, smoke coverage, and doc sync landed with the proposal per the propose-then-implement policy. `bash scripts/okf pending` reports an empty review inbox again.
- Dogfood harvest tooling (proposed ADR 0014), at the owner's direction, so future kit updates start from a delta instead of re-exploring target codebases the way this session's first analysis had to. New source-only `scripts/harvest-dogfood`: `add` registers an installed repo at its current HEAD, the default report prints per-repo commits and new `docs/log.md` lines since the recorded SHA (flagging lines that mention the kit or upstreaming), kit-managed script drift classified via the ADR 0013 manifest (matches kit / unedited older kit output / owner-edited), the target's `okf pending` inbox, the `kit_version` stamp vs the kit `VERSION`, uncommitted-change and second-agent-config notes; `mark` records the reviewed high-water mark. Read-only against targets. The registry deliberately lives in the git-ignored `.okf-kit-backups/` (override: `OKF_DOGFOOD_REGISTRY`): it holds absolute local paths, which the kit's own stale-reference scan forbids in tracked files — the committed-registry design was rejected in the ADR for exactly that reason, along with a template-shipped feedback convention (maintainer concern pushed into user repos) and assistant-memory-only tracking (not a repo-executable mechanism). The design leans on two existing guarantees: the docs-sync hook means the narrative delta is always in the target's `docs/log.md`, and ADR 0013's manifest makes file drift provable. New `docs/specs/dogfood-harvest.md` carries the contract; `make smoke-harvest` covers add/duplicate-add/zero-delta/committed-work/kit-mention-flag/owner-edited/mark against a temp install; packaging and validation specs, root `CLAUDE.md`, `docs/GOAL.md` constraints, the source map, and a README "Tracking dogfood repos" section stay in sync. Both real dogfood repos were registered at the exact commits this session's analysis covered, and the live report reproduced that analysis in one command (drift, owner-edited hooks, 1+4 pending ADRs, second-agent config). No `VERSION` bump: source-only tooling, nothing installed changes (ADR 0012 precedent).
- ADR 0013 accepted by the owner: flipped `status: proposed` to `accepted` in the ADR. The script-provenance behavior now binds future installer work; no implementation change was needed since the updater, installer seeding, smoke tests, and docs landed with the proposal per the propose-then-implement policy (the ADR 0011 acceptance precedent). Index entry and `docs/GOAL.md` milestone carried no proposed marker to remove; the 0.1.2 log entry below stays as written since it was accurate when logged. `bash scripts/okf pending` reports an empty review inbox, so the new SessionStart ADR-inbox note goes silent — its zero-case exercised for real on its first day.

- Kit 0.1.2: dogfood-driven hardening, at the owner's direction, from an analysis of where the two installed repos (repo-pulse and skywatch) landed after their goal loops. Both repos had independently fixed different subsets of the same shipped-hook defects, and both had grown an ad-hoc second-agent (Codex CLI) port for repo chores — the owner scoped that usage as optional and chore-only (main coding stays with Claude Code), which set the shape of the changes:
  - Shipped-hook union (the superset of both repos' downstream fixes, now kit-original): the Stop hook honors `stop_hook_active` from the hook stdin payload — after a prior block in the same stop cycle it warns on stderr and allows the stop, so a read-only session can never loop (the old header comment claimed "no infinite loop" without implementing it); file exclusions are end-anchored so lookalikes like `LICENSE-MIT` or `CLAUDE.md.bak` count as code instead of silently skipping the docs check; `.codex/` and `AGENTS.md` join the agent-config exclusion group (untracked second-agent config had been re-arming the Stop hook every turn in skywatch); both hooks resolve their root via `CLAUDE_PROJECT_DIR` → `CODEX_PROJECT_DIR` → cwd and say "the repo playbook (CLAUDE.md; AGENTS.md if present)" instead of naming one agent's file. All no-ops in Claude-only repos. Hooks spec updated; smoke matrix extended (loop-guard warn path, payload-without-flag still blocks, anchoring both directions, agent-config silence).
  - SessionStart ADR review inbox: the version hook now also counts ADRs whose first `status:` line is `proposed` (skipping the index and numbered candidates, mirroring `okf pending`) and injects the count as session-start context — five proposed ADRs had lingered post-goal across the two repos because the goal-met report was the only surface that mentioned them. Local and offline; silent at zero. Hooks spec and smoke coverage updated.
  - Kit-managed script provenance (proposed ADR 0013, extending the ADR 0012 manifest): `update-existing-repo` no longer unconditionally overwrites `scripts/okf` and the two hooks — it refreshes in place (after a backup) only content the manifest proves is unedited kit output, records identical content (the pre-manifest opt-in), and preserves owner-edited scripts with the kit version staged as a numbered candidate under "Needs review"; `create-new-repo` seeds the manifest at install. Without this, the next kit release would have silently reverted both repos' hook hardening into a git-ignored backup dir — repo-pulse's log had explicitly flagged that fear. New `make smoke-scripts` simulates releases against unedited, owner-edited, and no-manifest targets. Installer and validation specs, README, guide, and the template's kit-version policy describe the same semantics.
  - Loop refinements from skywatch's run: the goal interview confirms that tools named in verification steps are installed or marks the step owner-gated (its Mailpit-specific milestone verification stalled on an owner-gated install while a later milestone finished first); the goal-met proposal protocol lists standard repo hygiene (license, CI running the verification commands plus the stale-map check, dependency updates, badges) among candidate sources — both repos did exactly that hygiene as their first post-goal work; the starter `okf-map.yml` warns against one broad glob mapped to a stack ADR (skywatch rewrote its map per-module after the broad mapping produced only noise). Packaging spec carries the interview and proposal additions.
  - Optional second-agent recipe (README, echoed in the guide): the goal loop stays with Claude Code; a chore agent's committed config is tolerated by the hooks; ported playbooks need explicit session-start reads (no `@` import resolution) and an honest note that the env-file read denial is Claude Code-only; hook mirrors in another agent's config are the owner's to re-sync after kit upgrades. Documentation only — no installer flag and no shipped `AGENTS.md`; the site's loop/decision descriptions gained the matching hygiene-candidates and inbox-note lines.
  - `VERSION` bumped to 0.1.2 per ADR 0010 (installed behavior changed), so both stamped dogfood repos will see a drift note and can take the new updater path — their edited hooks now get preserved with candidates instead of clobbered. Verified: `make test` (including the new `smoke-scripts` target and extended hook matrix), `bash scripts/okf check-stale` current, `adr-suggest` quiet (path heuristics don't see installer edits; ADR 0013 written by judgment, per the ADR 0007 precedent).
- Workflow-skills parked candidate: evidence gate resolved. Skywatch's first loop — interview, six milestones, acceptance passes that caught real pre-goal bugs, goal-met report, post-goal proposals — executed cleanly from resident template prose, so per the parking rationale this is a context optimization, not a correctness fix. It stays parked at low priority for the owner to schedule or drop.

## 2026-07-09

- Known item, parked by the owner (a candidate for a future milestone, not scheduled): workflow skills for the installed template. Ship namespaced Claude Code skills (`.claude/skills/okf-*/SKILL.md` — goal-interview, adr-review, acceptance-pass, kit-upgrade) carrying the expanded episodic procedures, and put `templates/CLAUDE.md` on a diet: binding rules and one-line pointers stay resident, procedure detail loads on demand. Design rule: guardrails and anything the loop depends on never move into skills (they trigger probabilistically; a missed skill must degrade to today's one-liner, not to nothing). Decision-shaped — changes how the contract is delivered and interacts with ADR 0008's everything-resident stance, so it starts with an ADR. Deliberately sequenced after skywatch's first loop: if the interview and acceptance pass execute cleanly from resident prose, this is a context optimization; if they wobble, a correctness fix. Rejected model: shipping domain-expertise skill packs (per outside config-repo examples) — out of scope for a domain-agnostic workflow kit.
- Stale-candidate refresh in the updater (ADR 0012, accepted at the owner's direction): the repo-pulse upgrade showed candidate strata forming — a second updater run at kit 0.1.1 numbered past the stale 0.1.0-era `CLAUDE.2.md`/`docs/index.2.md` to `.3` files. `update-existing-repo` now records every candidate it writes in `.okf-kit-backups/candidate-manifest` (`sha256` + path) and, on later runs, refreshes in place (after a backup) a stale candidate whose digest proves it is the script's own unedited output, removing provably-ours duplicates; owner-edited or unrecorded candidates are never touched and keep the numbered-path behavior, and a content-matching skip records the candidate so pre-manifest installs opt in after one run. Rejected alternatives in the ADR: unconditional highest-candidate overwrite (owner merge work could vanish into an ignored backup dir), in-file markers (candidates must stay byte-identical to install output), a manifest outside `.okf-kit-backups/`. New `make smoke-candidates` simulates kit releases with a mutable kit copy: refresh on template change, numbering for owner-edited candidates. Installer/validation/packaging specs and README describe the behavior. No `VERSION` bump: updater-only mechanics — no installed-file content changed, so stamped repos gain nothing from a re-run and a bump would only produce noise drift notes (the ADR 0010 revisit concern).
- Installers now append `.DS_Store` to target `.gitignore` (kit 0.1.1): minutes after the skywatch install, the `check-stale` unmapped-file note flagged a stray Finder artifact the installed ignore set did not cover. Added to both installers, `verify-install`, the template bootstrap list, installer spec, README lists, and smoke tests; `VERSION` bumped to 0.1.1 per ADR 0010 since installed behavior changed — the first post-stamp release, so stamped installs (skywatch) exercise drift reporting for real.
- `okf pending` now skips installer-written numbered review candidates (`*.N.md`) alongside the index: the first run against the repo-pulse install flagged `docs/adr/index.2.md` — a review candidate, not a live ADR — as missing a status field. Helper spec and smoke tests updated (candidates of both index and ADR files are excluded from the scan). Found by dogfooding the updater on repo-pulse minutes after release.
- ADR 0011 accepted by the owner: flipped `status: proposed` to `accepted` in the ADR and removed the proposed marker from its index entry and the `docs/GOAL.md` milestone. The env-file read denial now binds future work; no reversal needed since the implementation was already in place per the propose-then-implement policy. `bash scripts/okf pending` reports an empty review inbox.
- Added `status: accepted` frontmatter to ADRs 0001-0009: the new `okf pending` missing-status flag caught, on its first run in this repo, that they declared acceptance only in their body section and were invisible to the `status:` review scan. Frontmatter-only formatting fix; no ADR content changed.
- Env-file read denial (proposed ADR 0011), at the owner's direction, after a `.claudeignore` question: Claude Code has no native `.claudeignore` (verified against current docs and upstream feature requests), so the kit ships the supported mechanism instead — `settings.json` now carries `permissions.deny` rules `Read(./.env)` and `Read(./**/.env)`, keeping on-disk secrets out of conversation context and turning part of the secrets guardrail from instruction into enforcement. The deny set deliberately avoids `.env.*` globs (deny rules cannot be negated; the committed `.env.example` must stay readable) and denies Read only (Claude cannot leak what it cannot read; a Write denial would block allowed scaffolding). `update-existing-repo` merges `permissions` rules like hooks (union by exact rule, target entries preserved); `verify-install` fails without the two rules; the installed template's Agent config names the denial and a new security guardrail forbids removing or working around it (stop and ask instead). Enforcement caveat updated everywhere it is stated (README, guide, site, packaging spec): the kit's mechanical gates are docs sync plus the env-read denial. Smoke tests cover the installed rules, the merge with a target's own custom deny rule, and merge idempotency. ADR 0011 was scaffolded with the new `okf new-adr` command (first real use) and is `status: proposed` for owner review; rejected alternatives recorded in it (a `.claudeignore` file — false security; a PreToolUse ignore hook — nonstandard; broader default deny patterns — false denials in repos the kit cannot know). Root `VERSION` stays 0.1.0: this lands in the same unreleased first-stamped release as ADR 0010.
- First-iteration capture upgrades, at the owner's direction, generalizing what the repo-pulse dogfood run had to invent mid-loop or only discovered after the goal was declared met (a user pasting a full URL into an input that only took a bare id exposed both a fixable gap and a scope question no milestone had covered):
  - Experience over contract: the goal interview gained an example-interactions question (2-3 realistic inputs for the primary interface, including a messy or wrong one, phrased per repo kind) and the goal loop gained a pre-goal-met acceptance pass — clean checkout, README quickstart, example interactions plus variants and wrong inputs — with findings logged and out-of-scope items feeding the candidate-milestone proposals. `templates/GOAL.md` adds an input-tolerance success-criteria hint, requires user-facing milestone verifications to name a rejected or edge input, and ends its backlog with a suggested README-quickstart milestone (clean-checkout reproduction) that the interview drafts by default. Extends the ADR 0007 contract via the packaging spec, per the post-goal-lifecycle precedent; no new ADR.
  - Bootstrap conventions installs kept re-inventing: the interview's verification question now states the canonical-command convention (a new repo's first milestone establishes `make test`/`make run` or the stack-native equivalent); the template bootstrap and both installers carry the env-file ignore set (`.env`, `.env.*`, `!.env.example`) alongside the three kit entries, with a committed-`.env.example` security guardrail and a stack-ignores-at-milestone-one instruction; starter `docs/index.md` now links the log and source map. `verify-install` requires the six ignore entries and checks `.env` is ignored while `.env.example` stays trackable.
  - Decision machinery: `scripts/okf` gained `new-adr` (next-numbered scaffold with `status: proposed` frontmatter, required sections, index entry), `new-spec` (purpose/contract/verification skeleton, index entry), and `pending` (the owner's review inbox: proposed ADRs plus a flag for ADRs missing a status field, which would otherwise escape the review scan); `adr-suggest` prints the matching scaffold command; the goal-met report must list pending proposed ADRs. `check-stale` additionally prints a non-blocking note for changed files with no mapping (hook mode unaffected), so new source areas get mapped as they gain contracts.
  - Kit version stamp and drift reporting (ADR 0010, accepted at the owner's direction): a source-only root `VERSION` file (starting at 0.1.0), installers stamping `kit_version` into target `docs/index.md`, the SessionStart hook comparing the stamp against the published `VERSION` and injecting a drift note, a Kit version policy section in the installed template recommending the safe updater, and a README "Upgrading an installed repo" section. Silent for unstamped repos; `verify-install` warns. This is the mechanism by which template lessons reach repos installed before them.
  - Synced knowledge and docs: packaging, installer, helper, hooks, and validation specs carry the new contracts; this repo's `docs/index.md` is stamped `kit_version: 0.1.0` and root `CLAUDE.md` lists `VERSION`; `docs/okf-map.yml` maps `VERSION` and adds ADR 0010 to the hook, installers, and template; README (loop description, install table, manual steps, daily use, commit-vs-ignore, hooks, helper commands, target verification), the guide's installed-template paragraph, and the website (helper table row, interview list, loop stop conditions, backlog-runs-out anatomy) describe the same behavior; Makefile smoke tests cover the stamp, env ignores, index links, scaffolds, pending, and the unmapped note, plus a `VERSION` semver check.
- Scripted the post-goal lifecycle, at the owner's direction, closing the dead end after "report the goal met and stop": `templates/CLAUDE.md` now has a goal-met proposal protocol (offer candidate next milestones sourced from `docs/log.md` known items, accepted-ADR revisit triggers, and extensions within the stated non-goals, with non-goal-revision options listed separately; nothing enters `docs/GOAL.md` without owner confirmation, and Claude drafts chosen milestones at the owner's direction) and explicit ADR review mechanics (scan for `status: proposed`; accepting flips the status and binds future work; rejecting reverts per the rollback trigger; the owner may direct Claude Code to make the edit and reversal, logged). `templates/GOAL.md` milestone preamble notes how the backlog grows. Extends ADR 0007 loop/decision semantics without moving any ownership boundary, so the packaging spec contract carries both additions and no new ADR was warranted. README Daily use, the guide, and the website (loop stop conditions, decisions section, and a new backlog-runs-out entry in the conversation anatomy) describe the same behavior. Motivated by the repo-pulse dogfood run, where the loop reached an empty backlog and the owner asked what to iterate on next.
- Added a prominent README website badge linking to the live GitHub Pages site. This only improves repo-homepage discoverability; the site source, Pages workflow, installer behavior, and documented kit contract did not change, so no spec or ADR edit was warranted.
- Retargeted the README Claude Code badge from the external product docs to this repo's "Daily use after installation" section so the badge explains the kit's Claude Code-specific workflow in place. Link-only documentation navigation change; no spec or ADR edit was warranted.

## 2026-07-08

- Added the autonomous iteration contract to `templates/CLAUDE.md` (ADR 0007), at the owner's direction, so any installed repo supports a fully spec-driven loop: the owner provides the goal, Claude Code makes and records the decisions, and iteration continues until the goal is met.
  - Goal iteration now loops: verify, check off, log, take the next milestone; stop on an empty backlog, a reserved decision, or an owner stop; report the goal met when all milestones and success criteria pass instead of inventing scope. A `docs/GOAL.md` or `CLAUDE.md` still holding template brackets gets filled with the owner before the first milestone task.
  - New decision policy: implementation choices inside specs, accepted ADRs, and guardrails are Claude Code's to make; decision-shaped changes start with a self-authored `status: proposed` ADR that is implemented against and flagged for asynchronous owner review; goal edits and accepted-ADR supersession stay with the owner.
  - New guardrails section: the test command runs after every change and gates milestone check-off; failing tests are never deleted, skipped, or weakened to force green; outcomes are reported honestly; no secrets in tracked files and env/credential files must be confirmed git-ignored; auth/session/input/file-path/network/crypto/permission changes go through `adr-suggest` and get a proposed ADR when flagged; new runtime dependencies need a proposed ADR; force pushes, data deletion or migration, out-of-scope file deletion, publishing, deploying, releasing, and external side-effecting calls need the owner's explicit go-ahead.
- Updated `docs/specs/repo-kit-packaging.md` with the installed-template contract and a README requirement to describe the loop and guardrails; updated README and guide descriptions to match (including the guide's CLAUDE.md excerpt); added ADR 0007 and indexed it; mapped `templates/CLAUDE.md` to ADR 0007 in `docs/okf-map.yml`.
- Added the matching milestone to `docs/GOAL.md` at the owner's direction and checked it off after `make test` passed. `scripts/okf adr-suggest` stayed quiet (its path heuristics do not match template edits); the ADR was written by judgment as an ownership-boundary change.
- Surfaced the ADR 0007 enforcement caveat in the user-facing docs, at the owner's direction: README ("What this kit does" and "How the hooks behave") and the guide now state plainly that the docs-sync hooks are the kit's only mechanical enforcement, the test/security/destructive-action guardrails are instruction-level in the installed `CLAUDE.md`, and repos needing guaranteed gates add them in their own CI and branch protection. The packaging spec now requires the caveat in both docs.
- Added the kit's website, at the owner's direction (ADR 0009): static, self-contained `site/index.html` on `main`, deployed to GitHub Pages by `.github/workflows/pages.yml` using the official Actions deployment (`configure-pages`, `upload-pages-artifact`, `deploy-pages`) on pushes touching `site/` or the workflow. The site documents how Claude Code works with an installed repo: session context via imports, the goal interview, the iteration loop, the decision policy, the guardrails with the enforcement caveat, interruption resume, and an anatomy-of-a-conversation section. Rejected alternatives recorded in the ADR: a `gh-pages` orphan branch (escapes the docs-sync and stale-map guardrails) and deploy-from-branch `/docs` (collides with the OKF knowledge bundle). Packaging spec lists `site/` and the workflow as source-only and carries the site content contract; `docs/okf-map.yml` maps both to the packaging spec and ADR 0009; README links the site and records the Pages source toggle (server-side, not reproducible from a clone) in Repository settings with a restore command. The Makefile stale-reference scan now covers `site/` — scan scope is not enumerated in the validation spec, so no spec edit was warranted for that. Publishing starts only after the owner enables Settings -> Pages -> Source: GitHub Actions, per the outward-facing-action guardrail.
- Clarified the `docs/okf-map.yml` fill expectation, at the owner's direction: the new-repo installer next steps and README (automated-setup placeholder note and manual step 8) now say the map fills during iteration as modules gain specs — Claude Code maintains it — rather than implying a day-one manual edit. Wording only; no installer behavior or contract changed, so no spec edit was warranted.
- Added worked examples to each goal-interview question in `templates/CLAUDE.md`, at the owner's direction: one running example (a design-tokens CLI) illustrates the expected shape of every answer, and a push-back instruction rejects answers that cannot be checked mechanically. Inline `Example:` prose was chosen over comment syntax — comments cost the same context when the file loads but hide in rendered views and invite deletion alongside the template comment. The packaging spec's interview contract now requires the examples and the push-back behavior.
- Made the goal interview explicit, at the owner's direction, instead of adding a wizard CLI (rejected: a Bash prompt flow collects filled brackets, not a verifiable goal; it is hard to test non-interactively; and it would drift from the in-session bootstrap — Claude Code is the better interviewer). `templates/CLAUDE.md` gained a Goal interview section with the ordered questions (what and for whom; concrete done state; mechanical verification; non-goals; fixed versus ADR-delegated stack choices; first shippable slice), drafting from code in existing repos and never overwriting owner-written goal content. Installer completion output now recommends the interview path with manual editing as the alternative (`create-new-repo`, `update-existing-repo`, `install-kit`); the packaging spec's ADR 0007 contract and the installer spec carry the requirements; README and guide describe both paths.
- Added a resume-after-interruption rule to the goal loop, at the owner's direction: at session start, uncommitted working-tree changes are treated as in-flight work from a cut-off session and reconciled against the first unchecked milestone and the newest `docs/log.md` entry — finished or backed out — before new work starts. Added to `templates/CLAUDE.md` and this repo's `CLAUDE.md`; the packaging spec carries it in the ADR 0007 contract list (an extension of the loop semantics, not a new decision, so no new ADR); README and guide describe it.
- Added session context preloading (ADR 0008), at the owner's direction: `templates/CLAUDE.md` and this repo's `CLAUDE.md` gained a Preloaded context section with `@` imports of `docs/GOAL.md`, `docs/specs/index.md`, and `docs/adr/index.md`, so the goal and knowledge indexes are guaranteed in context at session start instead of relying on instructed reads. Grounding and goal-iteration instructions were reworded to match (read only the specific governing spec/ADR per task); the bootstrap section notes imports resolve once the docs tree exists. The import list is deliberately closed — `docs/log.md`, `docs/okf-map.yml`, and full specs stay on disk. Packaging spec carries the import contract, validation spec and the new-repo smoke test verify the installed imports, README and guide describe the mechanism, and `docs/okf-map.yml` maps `templates/CLAUDE.md` to ADR 0008.

## 2026-07-07

- Dogfooded ADR 0006 in this source repo: added `docs/GOAL.md` with the kit's own goal, success criteria, non-goals, and milestone backlog (delivered milestones checked, backlog owner-managed); linked it from `docs/index.md`; root `CLAUDE.md` gained a Goal iteration section mirroring `templates/CLAUDE.md` and now distinguishes `docs/GOAL.md` (this repo's goal) from the `templates/GOAL.md` install artifact. No install behavior changed, so no spec or ADR edits were warranted.
- Added `templates/GOAL.md`, installed to target `docs/GOAL.md`: a goal template with repo kind (app, service, or utility), problem, target state, verifiable success criteria, non-goals, constraints, and an ordered milestone backlog (ADR 0006).
- Added a Goal iteration section to `templates/CLAUDE.md`: Claude Code reads `docs/GOAL.md` each session, takes the first unchecked milestone when asked to continue, checks it off only when its verification passes, and logs progress.
- Both installers now install `docs/GOAL.md`; the existing-repo installer preserves an existing file and writes a numbered candidate such as `docs/GOAL.2.md`.
- The existing-repo installer now skips writing a numbered candidate when the existing file already matches the kit content, keeping repeated updates idempotent for kit-created files.
- `verify-install` checks `docs/GOAL.md` exists; `check-placeholders` reports its unfilled template placeholders.
- Updated README, guide, packaging and installer specs, source map, and Makefile smoke tests to cover the goal file.
- Fixed `make scan` false pass: when ripgrep was not installed, the missing command looked like "no matches" and the scan reported success without running. The scan now falls back to `grep -rnEI` and fails on scanner errors instead of reporting success.
- QA pass: starter `docs/index.md` written by installers and README manual steps now links `GOAL.md` so the bundle root index is current on install; ADR 0006 and the installer spec now state that the identical-content candidate skip applies to all installer candidate writes; README and guide mention the skip.
- Ignore-policy audit: root `CLAUDE.md` ignore policy now includes the Python bytecode/cache entries that `.gitignore` and the packaging spec already had; `templates/CLAUDE.md` bootstrap now requires all 3 target ignore entries (adding `.okf-kit-backups/`) so bootstrapped repos pass `verify-install`. No `.gitignore` content changed; both files matched the intended policy.
- Added `.github/dependabot.yml` (weekly GitHub Actions version updates), the repo's only dependency surface. Considered and rejected Snyk: the kit has no dependency manifests, containers, or IaC, and Snyk Code does not analyze Bash; ShellCheck plus the Makefile smoke tests remain the security-relevant checks.
- Enabled this repo's GitHub-side security toggles (not reproducible from a clone): Dependabot alerts (`gh api -X PUT repos/OWNER/REPO/vulnerability-alerts`) and Dependabot security updates (`gh api -X PUT repos/OWNER/REPO/automated-security-fixes`); secret scanning and push protection were already on. Recorded them in a new README "Repository settings" section since server-side settings leave no tracked-file trace. These apply to this source repo only and are not installed into target repos.

## 2026-07-05

- Replaced setup examples with clone-and-`KIT="$(pwd)"` commands so docs do not depend on a local checkout path.
- Added a static README OKF validated badge that links to the kit validation section.
- Added a README Project Status section and linked the preview badge to it.
- Clarified that source-kit OKF validation covers helper behavior and stale mappings, while target repos own stricter OKF document-schema checks when needed.
- Made all README badges clickable.
- Made the README GitHub Actions test badge clickable.
- Removed the README license badge while keeping the MIT license file.
- Added safe install, verification, and placeholder helper scripts with Makefile smoke coverage.
- Clarified README setup paths, target-repo ignore rules, source-repo ignore rules, and OKF version hook behavior.
- Added a README table of contents for faster navigation.
- Changed the README status badge from alpha to preview.
- Added a clearer README opening section that explains what the kit does before installation details.
- Added Python bytecode/cache ignores and changed the syntax check to avoid writing bytecode.
- Added quality checks for optional ShellCheck linting, Markdown links, local-path scanning, installer idempotency, and GitHub Actions validation.
- Added an MIT license and README badges for the new `lilabrooks/claude-okf-repo-kit` GitHub repo.
- Updated README, guide, specs, and ADR index so the documented installer contract matches the safe script behavior.
- Strengthened installer behavior so `.gitignore` is appended, existing Markdown/map files are preserved, same-folder numbered candidates are created for name collisions, and script results print clearly.
- Updated ignore policy to include local env files and logs while keeping `.env.example` trackable.
- Split source repo instructions from the installable `CLAUDE.md` template. Root `CLAUDE.md` now governs this repo; `templates/CLAUDE.md` is copied into target repos.
- Added validation coverage proving installers copy `templates/CLAUDE.md`, not root `CLAUDE.md`.
- Clarified that installer scripts are the preferred pre-session setup path, while `CLAUDE.md` docs bootstrap remains the in-session fallback.
- Added safe installer scripts for creating new target repos and updating existing repos.
- Clarified source-kit verification and installed target repo verification in the README and guide.
- Added the initial OKF knowledge bundle for this repo: specs, ADRs, indexes, log, and source-to-doc mappings.
- Standardized on ADR naming. The user's "ARD" request maps to Architecture Decision Records, matching the repo guide.
