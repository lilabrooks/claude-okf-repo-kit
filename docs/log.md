# Log

## 2026-07-09

- Added a prominent README website badge linking to the live GitHub Pages site. This only improves repo-homepage discoverability; the site source, Pages workflow, installer behavior, and documented kit contract did not change, so no spec or ADR edit was warranted.

## 2026-07-08

- Added the autonomous iteration contract to `templates/CLAUDE.md` (ADR 0007), at the owner's direction, so any installed repo supports a fully spec-driven loop: the owner provides the goal, Claude Code makes and records the decisions, and iteration continues until the goal is met.
  - Goal iteration now loops: verify, check off, log, take the next milestone; stop on an empty backlog, a reserved decision, or an owner stop; report the goal met when all milestones and success criteria pass instead of inventing scope. A `docs/GOAL.md` or `CLAUDE.md` still holding template brackets gets filled with the owner before the first milestone task.
  - New decision policy: implementation choices inside specs, accepted ADRs, and guardrails are Claude Code's to make; decision-shaped changes start with a self-authored `status: proposed` ADR that is implemented against and flagged for asynchronous owner review; goal edits and accepted-ADR supersession stay with the owner.
  - New guardrails section: the test command runs after every change and gates milestone check-off; failing tests are never deleted, skipped, or weakened to force green; outcomes are reported honestly; no secrets in tracked files and env/credential files must be confirmed git-ignored; auth/session/input/file-path/network/crypto/permission changes go through `adr-suggest` and get a proposed ADR when flagged; new runtime dependencies need a proposed ADR; force pushes, data deletion or migration, out-of-scope file deletion, publishing, deploying, releasing, and external side-effecting calls need the owner's explicit go-ahead.
- Updated `docs/specs/repo-kit-packaging.md` with the installed-template contract and a README requirement to describe the loop and guardrails; updated README and guide descriptions to match (including the guide's CLAUDE.md excerpt); added ADR 0007 and indexed it; mapped `templates/CLAUDE.md` to ADR 0007 in `docs/okf-map.yml`.
- Added the matching milestone to `docs/GOAL.md` at the owner's direction and checked it off after `make test` passed. `scripts/okf adr-suggest` stayed quiet (its path heuristics do not match template edits); the ADR was written by judgment as an ownership-boundary change.
- Surfaced the ADR 0007 enforcement caveat in the user-facing docs, at the owner's direction: README ("What this kit does" and "How the hooks behave") and the guide now state plainly that the docs-sync hooks are the kit's only mechanical enforcement, the test/security/destructive-action guardrails are instruction-level in the installed `CLAUDE.md`, and repos needing guaranteed gates add them in their own CI and branch protection. The packaging spec now requires the caveat in both docs.
- Added the kit's website, at the owner's direction (ADR 0009): static, self-contained `site/index.html` on `main`, deployed to GitHub Pages by `.github/workflows/pages.yml` using the official Actions deployment (`configure-pages`, `upload-pages-artifact`, `deploy-pages`) on pushes touching `site/` or the workflow. The site documents how Claude Code works with an installed repo: session context via imports, the goal interview, the iteration loop, the decision policy, the guardrails with the enforcement caveat, interruption resume, and an anatomy-of-a-conversation section. Rejected alternatives recorded in the ADR: a `gh-pages` orphan branch (escapes the docs-sync and stale-map guardrails) and deploy-from-branch `/docs` (collides with the OKF knowledge bundle). Packaging spec lists `site/` and the workflow as source-only and carries the site content contract; `docs/okf-map.yml` maps both to the packaging spec and ADR 0009; README links the site and records the Pages source toggle (server-side, not reproducible from a clone) in Repository settings with a restore command. The Makefile stale-reference scan now covers `site/` — scan scope is not enumerated in the validation spec, so no spec edit was warranted for that. Publishing starts only after the owner enables Settings -> Pages -> Source: GitHub Actions, per the outward-facing-action guardrail.
- Clarified the `docs/okf-map.yml` fill expectation, at the owner's direction: the new-repo installer next steps and README (automated-setup placeholder note and manual step 8) now say the map fills during iteration as modules gain specs — Claude Code maintains it — rather than implying a day-one manual edit. Wording only; no installer behavior or contract changed, so no spec edit was warranted.
- Added worked examples to each goal-interview question in `templates/CLAUDE.md`, at the owner's direction: one running example (a design-tokens CLI) illustrates the expected shape of every answer, and a push-back instruction rejects answers that cannot be checked mechanically. Inline `Example:` prose was chosen over comment syntax — comments cost the same context when the file loads but hide in rendered views and invite deletion alongside the template comment. The packaging spec's interview contract now requires the examples and the push-back behavior.
- Made the goal interview explicit, at the owner's direction, instead of adding a wizard CLI (rejected: a Bash prompt flow collects filled brackets, not a verifiable goal; it is hard to test non-interactively; and it would drift from the in-session bootstrap — Claude Code is the better interviewer). `templates/CLAUDE.md` gained a Goal interview section with the ordered questions (what and for whom; concrete done state; mechanical verification; non-goals; fixed versus ADR-delegated stack choices; first shippable slice), drafting from code in existing repos and never overwriting owner-written goal content. Installer completion output now recommends the interview path with manual editing as the alternative (`create-new-repo`, `update-existing-repo`, `install-kit`); the packaging spec's ADR 0007 contract and the installer spec carry the requirements; README and guide describe both paths.
- Added a resume-after-interruption rule to the goal loop, at the owner's direction: at session start, uncommitted working-tree changes are treated as in-flight work from a cut-off session and reconciled against the first unchecked milestone and the newest `docs/log.md` entry — finished or backed out — before new work starts. Added to `templates/CLAUDE.md` and this repo's `CLAUDE.md`; the packaging spec carries it in the ADR 0007 contract list (an extension of the loop semantics, not a new decision, so no new ADR); README and guide describe it.
- Added session context preloading (ADR 0008), at the owner's direction: `templates/CLAUDE.md` and this repo's `CLAUDE.md` gained a Preloaded context section with `@` imports of `docs/GOAL.md`, `docs/specs/index.md`, and `docs/adr/index.md`, so the goal and knowledge indexes are guaranteed in context at session start instead of relying on instructed reads. Grounding and goal-iteration instructions were reworded to match (read only the specific governing spec/ADR per task); the bootstrap section notes imports resolve once the docs tree exists. The import list is deliberately closed — `docs/log.md`, `docs/okf-map.yml`, and full specs stay on disk. Packaging spec carries the import contract, validation spec and the new-repo smoke test verify the installed imports, README and guide describe the mechanism, and `docs/okf-map.yml` maps `templates/CLAUDE.md` to ADR 0008.

## 2026-07-07

- Dogfooded ADR 0006 in this source repo: added `docs/GOAL.md` with the kit's own goal, success criteria, non-goals, and milestone backlog (delivered milestones checked, backlog owner-managed); linked it from `docs/index.md`; root `CLAUDE.md` gained a Goal iteration section mirroring `templates/CLAUDE.md` and now distinguishes `docs/GOAL.md` (this repo's goal) from the `templates/GOAL.md` install artifact. No install behavior changed, so no spec or ADR edits were warranted.
- Added `templates/GOAL.md`, installed to target `docs/GOAL.md`: a goal template with repo kind (app, service, or utility), problem, target state, verifiable success criteria, non-goals, constraints, and an ordered milestone backlog (ADR 0006).
- Added a Goal iteration section to `templates/CLAUDE.md`: Claude Code reads `docs/GOAL.md` each session, takes the first unchecked milestone when asked to continue, checks it off only when its verification passes, and logs progress.
- Both installers now install `docs/GOAL.md`; the existing-repo installer preserves an existing file and writes a numbered candidate such as `docs/GOAL.2.md`.
- The existing-repo installer now skips writing a numbered candidate when the existing file already matches the kit content, keeping repeated updates idempotent for kit-created files.
- `verify-install` checks `docs/GOAL.md` exists; `check-placeholders` reports its unfilled template placeholders.
- Updated README, guide, packaging and installer specs, source map, and Makefile smoke tests to cover the goal file.
- Fixed `make scan` false pass: when ripgrep was not installed, the missing command looked like "no matches" and the scan reported success without running. The scan now falls back to `grep -rnEI` and fails on scanner errors instead of reporting success.
- QA pass: starter `docs/index.md` written by installers and README manual steps now links `GOAL.md` so the bundle root index is current on install; ADR 0006 and the installer spec now state that the identical-content candidate skip applies to all installer candidate writes; README and guide mention the skip.
- Ignore-policy audit: root `CLAUDE.md` ignore policy now includes the Python bytecode/cache entries that `.gitignore` and the packaging spec already had; `templates/CLAUDE.md` bootstrap now requires all 3 target ignore entries (adding `.okf-kit-backups/`) so bootstrapped repos pass `verify-install`. No `.gitignore` content changed; both files matched the intended policy.
- Added `.github/dependabot.yml` (weekly GitHub Actions version updates), the repo's only dependency surface. Considered and rejected Snyk: the kit has no dependency manifests, containers, or IaC, and Snyk Code does not analyze Bash; ShellCheck plus the Makefile smoke tests remain the security-relevant checks.
- Enabled this repo's GitHub-side security toggles (not reproducible from a clone): Dependabot alerts (`gh api -X PUT repos/OWNER/REPO/vulnerability-alerts`) and Dependabot security updates (`gh api -X PUT repos/OWNER/REPO/automated-security-fixes`); secret scanning and push protection were already on. Recorded them in a new README "Repository settings" section since server-side settings leave no tracked-file trace. These apply to this source repo only and are not installed into target repos.

## 2026-07-05

- Replaced setup examples with clone-and-`KIT="$(pwd)"` commands so docs do not depend on a local checkout path.
- Added a static README OKF validated badge that links to the kit validation section.
- Added a README Project Status section and linked the preview badge to it.
- Clarified that source-kit OKF validation covers helper behavior and stale mappings, while target repos own stricter OKF document-schema checks when needed.
- Made all README badges clickable.
- Made the README GitHub Actions test badge clickable.
- Removed the README license badge while keeping the MIT license file.
- Added safe install, verification, and placeholder helper scripts with Makefile smoke coverage.
- Clarified README setup paths, target-repo ignore rules, source-repo ignore rules, and OKF version hook behavior.
- Added a README table of contents for faster navigation.
- Changed the README status badge from alpha to preview.
- Added a clearer README opening section that explains what the kit does before installation details.
- Added Python bytecode/cache ignores and changed the syntax check to avoid writing bytecode.
- Added quality checks for optional ShellCheck linting, Markdown links, local-path scanning, installer idempotency, and GitHub Actions validation.
- Added an MIT license and README badges for the new `lilabrooks/claude-okf-repo-kit` GitHub repo.
- Updated README, guide, specs, and ADR index so the documented installer contract matches the safe script behavior.
- Strengthened installer behavior so `.gitignore` is appended, existing Markdown/map files are preserved, same-folder numbered candidates are created for name collisions, and script results print clearly.
- Updated ignore policy to include local env files and logs while keeping `.env.example` trackable.
- Split source repo instructions from the installable `CLAUDE.md` template. Root `CLAUDE.md` now governs this repo; `templates/CLAUDE.md` is copied into target repos.
- Added validation coverage proving installers copy `templates/CLAUDE.md`, not root `CLAUDE.md`.
- Clarified that installer scripts are the preferred pre-session setup path, while `CLAUDE.md` docs bootstrap remains the in-session fallback.
- Added safe installer scripts for creating new target repos and updating existing repos.
- Clarified source-kit verification and installed target repo verification in the README and guide.
- Added the initial OKF knowledge bundle for this repo: specs, ADRs, indexes, log, and source-to-doc mappings.
- Standardized on ADR naming. The user's "ARD" request maps to Architecture Decision Records, matching the repo guide.
